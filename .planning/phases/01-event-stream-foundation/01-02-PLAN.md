---
phase: 01-event-stream-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/pipeline/adapters/__init__.py
  - src/pipeline/adapters/claude_jsonl.py
  - src/pipeline/adapters/git_history.py
  - src/pipeline/normalizer.py
  - src/pipeline/storage/writer.py
autonomous: true

must_haves:
  truths:
    - "DuckDB read_json_auto() loads Claude Code JSONL files into queryable tables without Python line-by-line parsing"
    - "Actor is correctly identified: human messages vs tool results vs system meta (not all type:user as human)"
    - "Git history events are merged with JSONL events into a unified canonical event stream"
    - "Temporal alignment uses explicit commit hash links when available, falls back to ±2s windowing"
    - "Deterministic event_id ensures re-ingesting same files produces zero duplicates"
  artifacts:
    - path: "src/pipeline/adapters/claude_jsonl.py"
      provides: "DuckDB-based JSONL loading with SQL filtering and normalization"
      exports: ["load_jsonl_to_duckdb", "normalize_jsonl_events"]
    - path: "src/pipeline/adapters/git_history.py"
      provides: "Git log parsing into canonical events"
      exports: ["parse_git_history"]
    - path: "src/pipeline/normalizer.py"
      provides: "Event merging, deduplication, temporal alignment"
      exports: ["normalize_events"]
    - path: "src/pipeline/storage/writer.py"
      provides: "Write events to DuckDB with idempotent upsert"
      exports: ["write_events", "write_segments"]
  key_links:
    - from: "src/pipeline/adapters/claude_jsonl.py"
      to: "DuckDB read_json_auto"
      via: "SQL-based JSONL loading"
      pattern: "read_json_auto|read_json"
    - from: "src/pipeline/normalizer.py"
      to: "src/pipeline/models/events.py"
      via: "Creates CanonicalEvent instances"
      pattern: "CanonicalEvent"
    - from: "src/pipeline/storage/writer.py"
      to: "DuckDB events table"
      via: "INSERT with ON CONFLICT for idempotent ingestion"
      pattern: "ON CONFLICT.*IGNORE|INSERT OR IGNORE"
---

<objective>
Load raw session data into DuckDB using native JSONL reading, parse git history, and normalize everything into deduplicated canonical events with temporal alignment.

Purpose: This is the EXTRACT-01 implementation -- converting heterogeneous raw sources (JSONL files + git log) into a unified event stream. Uses DuckDB's `read_json_auto()` for JSONL reading per user architectural decision (NOT Python orjson line-by-line parsing).

Output: Working JSONL-to-DuckDB loading, git history parsing, event normalization with deduplication, and DuckDB writer with idempotent upsert.
</objective>

<execution_context>
@/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-event-stream-foundation/CLARIFICATIONS-ANSWERED.md
@.planning/phases/01-event-stream-foundation/01-RESEARCH.md
@.planning/phases/01-event-stream-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DuckDB-based JSONL loading and record normalization</name>
  <files>
    src/pipeline/adapters/__init__.py
    src/pipeline/adapters/claude_jsonl.py
  </files>
  <action>
    **CRITICAL USER DECISION: Use DuckDB for reading JSONL files.** Do NOT use orjson or Python line-by-line parsing. Use DuckDB's native `read_json_auto()` or `read_json()` to load JSONL files directly into DuckDB tables.

    **Create `src/pipeline/adapters/__init__.py`** as empty init file.

    **Create `src/pipeline/adapters/claude_jsonl.py`** with:

    `load_jsonl_to_duckdb(conn: duckdb.DuckDBPyConnection, jsonl_path: str | Path, session_id: str) -> int`:
    - Use `conn.execute("CREATE OR REPLACE TABLE raw_records AS SELECT * FROM read_json_auto(?)", [str(jsonl_path)])` to load the JSONL file
    - This leverages DuckDB's native JSONL parser which handles the heterogeneous record types
    - Return the count of loaded records
    - DuckDB will auto-detect the schema from the JSONL content

    `normalize_jsonl_events(conn: duckdb.DuckDBPyConnection, session_id: str) -> list[CanonicalEvent]`:
    - Query the raw_records table using SQL to transform JSONL records into canonical events
    - **CRITICAL: Correctly identify actors from JSONL type field (Pitfall 1 from research):**
      - `type='assistant'` -> actor='executor'
      - `type='user'` AND `isMeta=false` AND message.content does NOT contain tool_result blocks -> actor='human_orchestrator'
      - `type='user'` AND message.content contains tool_result blocks -> actor='tool'
      - `type='user'` AND `isMeta=true` -> actor='system' (skip or tag as metadata)
      - `type='system'` -> actor='system'
    - **Skip irrelevant record types:** Filter out `type IN ('progress', 'file-history-snapshot', 'queue-operation')` in SQL WHERE clause. These are ~45% of records and irrelevant to the pipeline.
    - **Map event_type from record content:**
      - assistant with thinking content block -> 'assistant_thinking'
      - assistant with text content block -> 'assistant_text'
      - assistant with tool_use content block -> 'tool_use'
      - user tool_result -> 'tool_result'
      - user human message -> 'user_msg'
      - system -> 'system_event'
    - **Extract payload structure** per Q7: Create payload.common (text, reasoning, tool_name, duration_ms, error_message, files_touched) + payload.details (tool-specific nested data)
    - **Extract links:** parent_uuid from parentUuid, tool_use_id from tool_result content blocks, source_tool_uuid from sourceToolAssistantUUID
    - **Extract commit hashes** from tool results that contain git commit output (regex: `\[[\w/.-]+\s+([0-9a-f]{7,40})\]`). Store in links.commit_hash.
    - **Handle assistant messages with multiple content blocks:** A single assistant JSONL record may have content blocks [thinking, text, tool_use]. Each block should produce a separate CanonicalEvent (up to 3 events per record). Use the record's uuid as the base, appending block index for uniqueness (e.g., uuid+"_0", uuid+"_1", uuid+"_2").
    - **Generate deterministic event_id** using CanonicalEvent.make_event_id() for each event.

    **Important boundary clarification:** DuckDB reads the JSONL files from disk (satisfies the user requirement that DuckDB handles file I/O, NOT Python line-by-line parsing). Python then transforms the DuckDB query results into CanonicalEvent objects (allowed -- Python is transforming in-memory query results, not reading files). DuckDB's JSON querying capabilities (json_extract, json_array_length, etc.) should be used to parse the nested JSONL structure where practical. For complex transformations that are awkward in SQL, it is acceptable to query raw data from DuckDB and transform the results in Python.

    Handle the `isSidechain: true` case by skipping those records (research recommendation for Phase 1).
  </action>
  <verify>
    Create a small test JSONL file with 5 records (1 human message, 1 assistant with tool_use, 1 tool result, 1 progress, 1 system) and verify:

    Run: `python -c "
import duckdb
from src.pipeline.adapters.claude_jsonl import load_jsonl_to_duckdb, normalize_jsonl_events
conn = duckdb.connect(':memory:')
# Use a real JSONL file if available, otherwise skip
import os
jsonl_dir = os.path.expanduser('~/.claude/projects/-Users-david-projects-orchestrator-policy-extraction/')
jsonl_files = [f for f in os.listdir(jsonl_dir) if f.endswith('.jsonl')]
if jsonl_files:
    count = load_jsonl_to_duckdb(conn, os.path.join(jsonl_dir, jsonl_files[0]), 'test-session')
    print(f'Loaded {count} raw records')
    events = normalize_jsonl_events(conn, 'test-session')
    print(f'Produced {len(events)} canonical events')
    actors = set(e.actor for e in events)
    print(f'Actors found: {actors}')
    types = set(e.event_type for e in events)
    print(f'Event types found: {types}')
else:
    print('No JSONL files found for testing')
"`
    Expected: Raw records loaded, canonical events produced with correct actor distribution (should include executor, tool, human_orchestrator), progress records filtered out.
  </verify>
  <done>
    DuckDB read_json_auto() loads JSONL files without Python line-by-line parsing. Actor identification correctly distinguishes human messages from tool results from system meta. Progress/snapshot/queue records are filtered out. Multiple content blocks per assistant message produce separate events. Commit hashes are extracted from git command output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Git history adapter and temporal alignment</name>
  <files>
    src/pipeline/adapters/git_history.py
    src/pipeline/normalizer.py
  </files>
  <action>
    **Create `src/pipeline/adapters/git_history.py`** with:

    `parse_git_history(repo_path: str | Path, since: str | None = None, until: str | None = None) -> list[CanonicalEvent]`:
    - Run `git log --format='%H|%aI|%an|%s' --name-only` via subprocess to get commit history
    - For each commit, create a CanonicalEvent with:
      - event_id: make_event_id('git', session_id_from_context, commit_hash, ts_utc, 'executor', 'git_commit')
      - actor: 'executor' (Claude Code makes commits)
      - event_type: 'git_commit'
      - payload.common: text=commit_message, files_touched=list_of_changed_files
      - payload.details.git: commit_hash, author, branch, files_changed
      - links.commit_hash: the full commit hash
      - source_system: 'git'
      - source_ref: commit_hash
    - Optional time range filtering with `--since` and `--until` flags
    - Parse ISO 8601 timestamps from git (author date with timezone) and normalize to UTC

    **Create `src/pipeline/normalizer.py`** with:

    `normalize_events(jsonl_events: list[CanonicalEvent], git_events: list[CanonicalEvent], config: PipelineConfig) -> list[CanonicalEvent]`:
    - **Merge** JSONL and git events into a single list
    - **Temporal alignment** (Q1 locked decision -- hybrid approach):
      1. For each git_commit event, search JSONL events for a tool_result containing the same commit hash in links.commit_hash. If found, this is an EXPLICIT link (confidence=1.0). Set the git event's timestamp to the tool_result's timestamp + 1ms for correct ordering.
      2. If no explicit link, search for JSONL tool_use events with 'git commit' in the command within ±causal_window_seconds (default 2s from config). If found, this is a WINDOWING link (confidence=0.8). Force git event to occur immediately after the matched JSONL event.
      3. If no match at all, keep original timestamp with confidence=0.0.
      4. Store link confidence in links.alignment_confidence field.
    - **Sort** by ts_utc (stable sort preserving line order for same-timestamp events)
    - **Deduplicate** (Q14 locked decision): Check for duplicate event_ids. For duplicates:
      - Keep the first occurrence
      - Log at DEBUG level per Q15
      - Track duplicate count; if >5% of total, log WARNING per Q15
    - **Temporal anomaly handling** (Q18): For events with identical timestamps, add deterministic microsecond noise: hash(event_id) % 1000 microseconds. Flag with temporal_anomaly metadata.
    - **Track ingestion metadata** (Q13): first_seen, last_seen, ingestion_count fields

    Use loguru for structured logging. Import from `src.pipeline.models.config` and `src.pipeline.models.events`.
  </action>
  <verify>
    Run: `python -c "
from src.pipeline.adapters.git_history import parse_git_history
events = parse_git_history('.')
print(f'Git events: {len(events)}')
if events:
    print(f'First commit: {events[0].payload.get(\"common\", {}).get(\"text\", \"?\")[:60]}')
    print(f'Actor: {events[0].actor}')
    print(f'Has commit_hash: {\"commit_hash\" in events[0].links}')
"`
    Expected: Git events loaded from current repo with correct actor and commit hash links.

    Run: `python -c "
from src.pipeline.normalizer import normalize_events
from src.pipeline.models.config import load_config
config = load_config()
# Test deduplication with duplicate events
from src.pipeline.models.events import CanonicalEvent
from datetime import datetime, timezone
e1 = CanonicalEvent(event_id='abc123', ts_utc=datetime.now(timezone.utc), session_id='s1', actor='executor', event_type='tool_use', payload={}, links={}, source_system='claude_jsonl', source_ref='test:1')
e2 = CanonicalEvent(event_id='abc123', ts_utc=datetime.now(timezone.utc), session_id='s1', actor='executor', event_type='tool_use', payload={}, links={}, source_system='claude_jsonl', source_ref='test:1')
result = normalize_events([e1, e2], [], config)
print(f'Input: 2 events (1 duplicate), Output: {len(result)} events')
"`
    Expected: Input: 2 events (1 duplicate), Output: 1 events
  </verify>
  <done>
    Git history parsed into canonical events with commit hashes as links. Temporal alignment merges git + JSONL using explicit links (confidence=1.0) with ±2s windowing fallback (confidence=0.8). Deduplication correctly eliminates events with identical event_ids. Temporal anomalies handled with deterministic microsecond noise.
  </done>
</task>

<task type="auto">
  <name>Task 3: DuckDB event writer with idempotent upsert</name>
  <files>
    src/pipeline/storage/writer.py
  </files>
  <action>
    **Create `src/pipeline/storage/writer.py`** with:

    `write_events(conn: duckdb.DuckDBPyConnection, events: list[CanonicalEvent], tagged_events: list[TaggedEvent] | None = None) -> dict`:
    - Write canonical events to the DuckDB events table
    - If tagged_events provided, include primary_tag, primary_tag_confidence, secondary_tags columns
    - **Idempotent upsert** (Q7/Q14 locked decision): Use DuckDB's INSERT ... ON CONFLICT(event_id) DO UPDATE SET
      - On conflict: update last_seen = current_timestamp, increment ingestion_count
      - This ensures re-ingesting same JSONL files is safe
    - Use batch INSERT for performance (not one-by-one)
    - Serialize payload, links, risk_factors, secondary_tags as JSON strings for DuckDB JSON columns
    - Return stats dict: {inserted: N, updated: N, total: N}
    - **Log duplicates** at DEBUG level per Q15. If duplicate rate >5%, log WARNING.

    `write_segments(conn: duckdb.DuckDBPyConnection, segments: list[EpisodeSegment]) -> dict`:
    - Write episode segments to the episode_segments table
    - Similar idempotent upsert on segment_id
    - Serialize event_ids list as JSON
    - Return stats dict

    `read_events(conn: duckdb.DuckDBPyConnection, session_id: str | None = None, tag: str | None = None) -> list[dict]`:
    - Query events table with optional filtering
    - Return as list of dicts (for use in tagger and segmenter)
    - Parse JSON columns back to Python dicts

    `get_event_stats(conn: duckdb.DuckDBPyConnection) -> dict`:
    - Return aggregate stats: total events, events by actor, events by type, events by tag, duplicate count
    - Uses DuckDB aggregate queries

    **Error handling** (Q16 locked decision):
    - If >10% of events in a batch fail to insert (per abort_scope=per_session), raise an exception with detailed error report
    - Use config.validation.mode to determine strictness: strict mode raises on warnings, permissive mode logs and continues
  </action>
  <verify>
    Run: `python -c "
import duckdb
from src.pipeline.storage.schema import get_connection, create_schema
from src.pipeline.storage.writer import write_events, get_event_stats
from src.pipeline.models.events import CanonicalEvent
from datetime import datetime, timezone

conn = get_connection(':memory:')
create_schema(conn)

e1 = CanonicalEvent(
    event_id='test001', ts_utc=datetime.now(timezone.utc), session_id='s1',
    actor='executor', event_type='tool_use',
    payload={'common': {'text': 'test'}, 'details': {}},
    links={}, source_system='claude_jsonl', source_ref='test:1'
)
stats = write_events(conn, [e1])
print(f'First write: {stats}')

# Write same event again (idempotent)
stats2 = write_events(conn, [e1])
print(f'Re-write (idempotent): {stats2}')

# Check ingestion_count incremented
row = conn.execute('SELECT ingestion_count FROM events WHERE event_id = ?', ['test001']).fetchone()
print(f'Ingestion count after re-write: {row[0]}')

overall = get_event_stats(conn)
print(f'Total events: {overall[\"total\"]}')
"`
    Expected: First write shows 1 inserted. Re-write shows 0 inserted, 1 updated. Ingestion count = 2. Total events = 1.
  </verify>
  <done>
    Events and segments write to DuckDB with idempotent upsert. Re-ingesting same data increments ingestion_count without creating duplicates. Batch insert for performance. JSON serialization for nested columns. Error handling respects validation mode and abort threshold.
  </done>
</task>

</tasks>

<verification>
1. Load a real Claude Code JSONL file via DuckDB `read_json_auto()` -- no Python line-by-line parsing
2. Verify actor distribution includes executor, tool, human_orchestrator (not all human_orchestrator)
3. Progress records filtered out (~40% of raw records should be excluded)
4. Git history events have commit_hash in links
5. Temporal alignment assigns confidence scores to git-JSONL links
6. Writing same events twice produces identical DB state (idempotent)
7. Duplicate detection logs correctly at DEBUG/WARNING levels
</verification>

<success_criteria>
- JSONL files load via DuckDB native JSONL reading (not orjson/Python parsing)
- Actor identification correctly handles the three subtypes of type:"user" records
- Git events carry commit_hash links for temporal alignment
- Temporal alignment implements hybrid approach (explicit links preferred, windowing fallback)
- Deduplication prevents duplicate events on re-ingestion
- DuckDB writer supports idempotent upsert with ingestion metadata tracking
</success_criteria>

<output>
After completion, create `.planning/phases/01-event-stream-foundation/01-02-SUMMARY.md`
</output>
