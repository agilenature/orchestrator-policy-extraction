---
phase: 09-obstacle-escalation-detection
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extract_escalation_fixtures.py
  - tests/fixtures/escalation/session_01695e90_ocorr_trisky.jsonl
  - tests/fixtures/escalation/session_0326bf5e_ocorr_trisky.jsonl
  - tests/fixtures/escalation/session_1cf6d12f_ocorr_tgitcommit.jsonl
  - tests/fixtures/escalation/session_0e3cf9a0_window_expired.jsonl
  - tests/fixtures/escalation/session_1cf6d12f_ocorr_trisky.jsonl
  - tests/test_escalation_real_fixtures.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "At least 3 JSONL fixture files in tests/fixtures/escalation/ contain event slices extracted from real objectivism session data in data/ope.db"
    - "At least 3 positive fixture slices produce EscalationCandidate detections when run through EscalationDetector"
    - "At least 1 fixture slice demonstrates a real-session negative case (window expired or no bypass)"
    - "Extraction script documents provenance: which session_id, event_ids, and DuckDB query produced each fixture"
  artifacts:
    - path: "tests/fixtures/escalation/session_01695e90_ocorr_trisky.jsonl"
      provides: "Real session escalation: O_CORR -> T_RISKY at gap=1 (exempt-only intervening)"
    - path: "tests/fixtures/escalation/session_0326bf5e_ocorr_trisky.jsonl"
      provides: "Real session escalation: O_CORR -> T_RISKY at gap=1 (exempt-only intervening)"
    - path: "tests/fixtures/escalation/session_1cf6d12f_ocorr_tgitcommit.jsonl"
      provides: "Real session escalation: O_CORR -> T_GIT_COMMIT via Edit then Bash"
    - path: "tests/test_escalation_real_fixtures.py"
      provides: "Pytest file loading JSONL fixtures and running EscalationDetector.detect()"
  key_links:
    - from: "tests/test_escalation_real_fixtures.py"
      to: "tests/fixtures/escalation/*.jsonl"
      via: "json.loads per line, construct TaggedEvent objects"
      pattern: "fixtures/escalation/.*\\.jsonl"
    - from: "tests/test_escalation_real_fixtures.py"
      to: "src/pipeline/escalation/detector.py"
      via: "EscalationDetector.detect(tagged_events)"
      pattern: "detector\\.detect"
---

<objective>
Close the VERIFICATION.md gap on Truth 5: "confirmed positive examples from objectivism sessions."

Extract real O_CORR->T_RISKY and O_CORR->T_GIT_COMMIT event slices from the project's DuckDB (data/ope.db) into minimal JSONL fixture files, then add a test that loads these fixtures and confirms EscalationDetector detects them correctly.

Purpose: Prove the detector algorithm works on real-session event patterns, not just synthetic ones. This is a documentation/evidence gap -- the detector is already correct and fully tested with 40 synthetic tests. This plan adds the "confirmed from objectivism sessions" qualifier.

Output: 4-5 JSONL fixture files in tests/fixtures/escalation/, a one-off extraction script, and a new test file.
</objective>

<execution_context>
@/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-obstacle-escalation-detection/09-VERIFICATION.md

# Key source files to understand data models
@src/pipeline/escalation/detector.py
@src/pipeline/models/events.py
@tests/conftest.py (make_event, make_tagged_event helpers)
@tests/test_escalation_detector.py (existing test patterns to follow)
@src/pipeline/models/config.py (EscalationConfig with window_turns=5)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract real event slices from DuckDB into JSONL fixture files</name>
  <files>
    scripts/extract_escalation_fixtures.py
    tests/fixtures/escalation/session_01695e90_ocorr_trisky.jsonl
    tests/fixtures/escalation/session_0326bf5e_ocorr_trisky.jsonl
    tests/fixtures/escalation/session_1cf6d12f_ocorr_tgitcommit.jsonl
    tests/fixtures/escalation/session_0e3cf9a0_window_expired.jsonl
    tests/fixtures/escalation/session_1cf6d12f_ocorr_trisky.jsonl
  </files>
  <action>
    Create a one-off Python script `scripts/extract_escalation_fixtures.py` that:

    1. Connects to `data/ope.db` (DuckDB, read-only).
    2. For each confirmed sequence below, extracts a context window of events: from 1 event before the O_CORR through 2 events after the bypass event (by ts_utc order). This captures the minimal slice the detector needs.
    3. Writes each slice as a JSONL file (one JSON object per line) to `tests/fixtures/escalation/`.

    **Confirmed sequences to extract (from DB investigation):**

    **Positive fixtures (detector should find escalation):**
    - `session_01695e90_ocorr_trisky.jsonl`: Session `01695e90-4f8b-43a8-9104-2c64c2c39058`, O_CORR event `cd8a4ddb45e9ec7e` -> T_RISKY event `cf50a94e03b14f4e`. Gap=1 non-exempt turn (all intervening are Read = exempt). Extract events from O_CORR through T_RISKY + 1 event after.
    - `session_0326bf5e_ocorr_trisky.jsonl`: Session `0326bf5e...`, O_CORR event `f25edf2c...` -> T_RISKY event `6d8e58aa...`. Same pattern: gap=1 non-exempt turn. Extract O_CORR through T_RISKY + 1 event after.
    - `session_1cf6d12f_ocorr_tgitcommit.jsonl`: Session `1cf6d12f...`, O_CORR event `840d0c5a...` -> T_GIT_COMMIT event `4a59693a...` (tool=Bash). Intermediate events include Read (exempt) and Edit (non-exempt, bypass-eligible -- detector will match Edit first). Extract O_CORR through T_GIT_COMMIT + 1 event after.
    - `session_1cf6d12f_ocorr_trisky.jsonl`: Session `1cf6d12f...`, O_CORR event `9f8886ac...` -> T_RISKY event `263b105b...`. Larger gap with Bash/TaskOutput/TaskStop tools intervening. Some tools (TaskOutput, TaskStop) are NOT in the exempt list, so they count toward window_turns. Count non-exempt events carefully. If >5 non-exempt events intervene, this becomes a negative case with default window_turns=5 (test with window_turns=20 to make positive).

    **Negative fixture (detector should NOT find escalation with default config):**
    - `session_0e3cf9a0_window_expired.jsonl`: Session `0e3cf9a0...`, O_CORR event `f68c16da...` -> T_RISKY event `aae5c9e0...`. ~15 non-exempt events intervene (Bash calls, assistant_text, tool_results). With default window_turns=5, this window expires before the T_RISKY event. Detector should return empty list.

    **JSONL format per line** -- each line is one event object with these fields:
    ```json
    {
      "event_id": "cd8a4ddb45e9ec7e",
      "ts_utc": "2026-02-03T17:10:25.523000-06:00",
      "session_id": "01695e90-4f8b-43a8-9104-2c64c2c39058",
      "actor": "human_orchestrator",
      "event_type": "user_msg",
      "primary_tag": "O_CORR",
      "primary_tag_confidence": 0.8,
      "secondary_tags": [],
      "payload": {"common": {"text": "[truncated]", "tool_name": ""}},
      "links": {},
      "risk_score": 0.0,
      "risk_factors": [],
      "source_system": "claude_jsonl",
      "source_ref": "extracted_from_ope_db"
    }
    ```

    **Critical payload handling:**
    - TRUNCATE `payload.common.text` to max 200 chars (real payloads are 5000-15000 chars, unnecessary for detection). Append "[...truncated]" if truncated.
    - PRESERVE `payload.common.tool_name` exactly (needed for exempt/bypass checks).
    - PRESERVE `payload.details.file_path` if present (used for bypass_resource).
    - For tool_result events that lack `tool_name` in payload, leave it as empty string -- the detector handles this correctly (it uses tag-based bypass for T_RISKY/T_GIT_COMMIT).
    - Convert DuckDB timestamps to ISO 8601 strings.
    - Convert DuckDB JSON columns (secondary_tags, payload, links, risk_factors) to proper Python dicts/lists before JSON serialization.

    **Provenance comment:** Add a comment block at the top of each JSONL file (lines starting with `#`) documenting: session_id, block event_id, bypass event_id, extraction date, and the DuckDB query used.

    Run the script once to generate the fixtures. Verify each file exists and has the expected number of event lines.
  </action>
  <verify>
    - `python scripts/extract_escalation_fixtures.py` runs without error
    - `ls tests/fixtures/escalation/*.jsonl` shows 5 JSONL files
    - `wc -l tests/fixtures/escalation/*.jsonl` shows reasonable line counts (5-25 lines per file)
    - `python -c "import json; [json.loads(l) for l in open('tests/fixtures/escalation/session_01695e90_ocorr_trisky.jsonl') if not l.startswith('#')]"` succeeds (valid JSON)
  </verify>
  <done>
    5 JSONL fixture files exist in tests/fixtures/escalation/, each containing a real event slice from data/ope.db with truncated payloads and full provenance comments. Files are valid JSONL (ignoring comment lines).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pytest test that loads real fixtures and runs EscalationDetector</name>
  <files>
    tests/test_escalation_real_fixtures.py
  </files>
  <action>
    Create `tests/test_escalation_real_fixtures.py` that:

    1. **Fixture loader helper** `load_fixture(filename: str) -> list[TaggedEvent]`:
       - Reads `tests/fixtures/escalation/{filename}`, skipping lines starting with `#`.
       - For each JSON line, constructs a `CanonicalEvent` with the fields from the JSONL.
       - Then wraps each in a `TaggedEvent` using the `primary_tag` and `primary_tag_confidence` from the JSONL (if primary_tag is non-empty, create a Classification with source="direct"; otherwise primary=None).
       - Returns a list of TaggedEvent objects in timestamp order.
       - Handle `ts_utc` parsing: the ISO string from JSONL must be parsed to a timezone-aware datetime. Use `datetime.fromisoformat()` (Python 3.11+).

    2. **Positive detection tests** (one per positive fixture):

       `test_session_01695e90_detects_escalation`:
       - Load `session_01695e90_ocorr_trisky.jsonl`
       - Create detector with `EscalationConfig(window_turns=10)` -- real session has 6 non-exempt events (4 tool_result + 1 assistant_text + T_RISKY itself); requires window_turns >= 6. Default window_turns=5 is too small.
       - Call `detector.detect(tagged_events)`
       - Assert `len(candidates) >= 1`
       - Assert the first candidate has `block_event_tag == "O_CORR"` and `session_id` starts with `01695e90`
       - Assert `block_event_id == "cd8a4ddb45e9ec7e"`

       `test_session_0326bf5e_detects_escalation`:
       - Load `session_0326bf5e_ocorr_trisky.jsonl`
       - Create detector with default config (window_turns=5) -- this fixture has few enough non-exempt events to fit within the default window
       - Assert `len(candidates) >= 1`
       - Assert `block_event_id` starts with `f25edf2c`

       `test_session_1cf6d12f_tgitcommit_detects_escalation`:
       - Load `session_1cf6d12f_ocorr_tgitcommit.jsonl`
       - Create detector with `EscalationConfig(window_turns=10)` -- Edit event is the 6th non-exempt event between O_CORR and T_GIT_COMMIT; requires window_turns >= 6. Default window_turns=5 is too small.
       - Assert `len(candidates) >= 1`
       - Assert block_event_id starts with `840d0c5a`
       - Note: The Edit tool_use (`fbb0aefe`) between O_CORR and T_GIT_COMMIT is bypass-eligible (tool name "Edit" is in bypass_eligible_tools). The detector will match on Edit before reaching the T_GIT_COMMIT. Assert the bypass event ID is the Edit event, not the T_GIT_COMMIT.

       `test_session_1cf6d12f_trisky_detects_with_larger_window`:
       - Load `session_1cf6d12f_ocorr_trisky.jsonl`
       - First: test with default config (window_turns=5). Count non-exempt events in the fixture to determine expected result. If >5 non-exempt before the T_RISKY, assert `len(candidates) == 0` with default config. Then create config with `window_turns=20` and assert `len(candidates) >= 1`.
       - OR if <=5 non-exempt: assert `len(candidates) >= 1` with default config.
       - (The test verifies the detector handles real-world gap sizes correctly.)

    3. **Negative detection test:**

       `test_session_0e3cf9a0_window_expired`:
       - Load `session_0e3cf9a0_window_expired.jsonl`
       - Create detector with default config (window_turns=5)
       - Assert `len(candidates) == 0` (window expired before T_RISKY reached)
       - Then create detector with window_turns=20 and assert `len(candidates) >= 1` (proves the sequence IS a real escalation, just beyond default window)

    4. **Parametrized summary test:**

       `test_all_positive_fixtures_detected`:
       - Use `@pytest.mark.parametrize` over (filename, window_turns) tuples since different fixtures require different window sizes:
         ```python
         @pytest.mark.parametrize("filename,window_turns", [
             ("session_01695e90_ocorr_trisky.jsonl", 10),
             ("session_0326bf5e_ocorr_trisky.jsonl", 5),
             ("session_1cf6d12f_ocorr_tgitcommit.jsonl", 10),
         ])
         ```
       - For each: load fixture, create detector with `EscalationConfig(window_turns=window_turns)`, assert at least 1 candidate found
       - This provides a compact summary assertion that each positive fixture is detected with its correct config

    **Import pattern:** Follow the existing test file structure:
    ```python
    from src.pipeline.escalation.detector import EscalationDetector
    from src.pipeline.models.config import PipelineConfig, EscalationConfig
    from src.pipeline.models.events import CanonicalEvent, Classification, TaggedEvent
    ```

    **DO NOT modify** any existing files (detector.py, events.py, conftest.py, test_escalation_detector.py). This plan only ADDS new files.
  </action>
  <verify>
    - `python -m pytest tests/test_escalation_real_fixtures.py -v` passes all tests
    - `python -m pytest tests/test_escalation_real_fixtures.py -v --tb=short 2>&1 | grep -c PASSED` shows at least 5 passed tests
    - `python -m pytest tests/ -v --tb=short 2>&1 | tail -3` shows no regressions (all 529+ existing tests still pass)
  </verify>
  <done>
    tests/test_escalation_real_fixtures.py exists with at least 5 test functions that load JSONL fixtures from tests/fixtures/escalation/ and run them through EscalationDetector. Positive fixtures produce detections with per-fixture window_turns configs (session_0326bf5e uses default window_turns=5; session_01695e90 and session_1cf6d12f use window_turns=10). The negative fixture (window expired) confirms no detection with default config. Total test count is 529+ with zero regressions.
  </done>
</task>

</tasks>

<verification>
1. `ls tests/fixtures/escalation/*.jsonl | wc -l` >= 4 (at least 4 JSONL fixture files)
2. `python -m pytest tests/test_escalation_real_fixtures.py -v` all pass
3. `python -m pytest tests/ --tb=short` all pass (no regressions)
4. `grep -l "session_id" tests/fixtures/escalation/*.jsonl | head -1 | xargs head -5` shows provenance comments and real session data
5. `grep "ope.db\|objectivism\|extracted" tests/fixtures/escalation/*.jsonl | head -3` confirms provenance documentation
</verification>

<success_criteria>
- Gap closure for VERIFICATION.md Truth 5: "confirmed positive examples from objectivism sessions" is now evidenced by JSONL fixtures extracted from real DuckDB session data
- At least 3 positive fixture files contain real O_CORR->T_RISKY/T_GIT_COMMIT sequences from objectivism sessions
- At least 1 negative fixture demonstrates window expiry on a real session sequence
- A dedicated test file loads these fixtures and confirms the EscalationDetector produces correct results
- Zero regressions in the existing 529-test suite
</success_criteria>

<output>
After completion, create `.planning/phases/09-obstacle-escalation-detection/09-05-SUMMARY.md`
</output>
