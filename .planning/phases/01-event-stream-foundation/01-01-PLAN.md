---
phase: 01-event-stream-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/config.yaml
  - src/pipeline/__init__.py
  - src/pipeline/models/__init__.py
  - src/pipeline/models/config.py
  - src/pipeline/models/events.py
  - src/pipeline/models/segments.py
  - src/pipeline/storage/__init__.py
  - src/pipeline/storage/schema.py
  - requirements.txt
autonomous: true

must_haves:
  truths:
    - "Configuration loads from data/config.yaml with all locked decision values (30s timeout, 0.7 risk threshold, keyword lists, label definitions)"
    - "Pydantic models validate event data and reject malformed records with clear error messages"
    - "DuckDB schema creates events and episode_segments tables with correct column types"
  artifacts:
    - path: "data/config.yaml"
      provides: "Complete config with all 21 locked decisions encoded"
      contains: "episode_timeout_seconds: 30"
    - path: "src/pipeline/models/config.py"
      provides: "PipelineConfig Pydantic model with validation + load_config()"
      exports: ["PipelineConfig", "load_config"]
    - path: "src/pipeline/models/events.py"
      provides: "CanonicalEvent, TaggedEvent, Classification Pydantic models"
      exports: ["CanonicalEvent", "TaggedEvent", "Classification"]
    - path: "src/pipeline/models/segments.py"
      provides: "EpisodeSegment Pydantic model"
      exports: ["EpisodeSegment"]
    - path: "src/pipeline/storage/schema.py"
      provides: "DuckDB table creation functions"
      exports: ["create_schema", "get_connection"]
  key_links:
    - from: "src/pipeline/models/config.py"
      to: "data/config.yaml"
      via: "PyYAML load + Pydantic validation"
      pattern: "yaml\\.safe_load.*PipelineConfig"
    - from: "src/pipeline/storage/schema.py"
      to: "src/pipeline/models/events.py"
      via: "Schema columns match model fields"
      pattern: "CREATE TABLE.*events"
---

<objective>
Build the foundation layer: configuration system, data models, and DuckDB schema that all subsequent plans depend on.

Purpose: Every downstream component (JSONL loading, tagger, segmenter, storage) needs shared config, validated data models, and a database schema. This plan establishes those shared dependencies first.

Output: Working config loader, immutable Pydantic models for all pipeline data types, and DuckDB schema creation functions.
</objective>

<execution_context>
@/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-event-stream-foundation/CLARIFICATIONS-ANSWERED.md
@.planning/phases/01-event-stream-foundation/01-RESEARCH.md
@data/config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Rewrite data/config.yaml with all locked decisions</name>
  <files>
    data/config.yaml
    src/pipeline/__init__.py
    src/pipeline/models/__init__.py
  </files>
  <action>
    **Rewrite `data/config.yaml`** to match the COMPLETE config structure from CLARIFICATIONS-ANSWERED.md Configuration Summary section. The existing config.yaml has an outdated structure (idle_timeout_minutes: 30, wrong section names). Replace it entirely with the locked structure:

    Required top-level sections:
    - `episode_timeout_seconds: 30` (Q4 locked decision -- NOT 30 minutes)
    - `classification:` with `labels:` (X_PROPOSE and X_ASK definitions from Q5 with canonical_examples and non_examples), `reaction_keywords:` (O_CORR keywords from Q6), `min_confidence: 0.5` (Q9)
    - `risk_model:` with `threshold: 0.7` (Q12), `combination_mode:` (Q11: max for classification, weighted_average for scoring), `risky_tools:` list, `protected_paths:` list, `false_positive_tolerance: true`
    - `payload:` with `common_fields:` (Q7: required=[text], optional=[reasoning, tool_name, duration_ms, error_message, files_touched])
    - `temporal:` with `causal_window_seconds: 2` (Q1), `link_confidence:` (explicit=1.0, windowing=0.8, none=0.0)
    - `deduplication:` with `track_metadata: true` (Q13), `log_duplicates: true` (Q15), `duplicate_alert_threshold: 0.05`
    - `validation:` with `mode: strict` (Q17), `invalid_event_abort_threshold: 0.10` (Q16), `abort_scope: per_session`
    - `temporal_anomalies:` with `tolerate: true`, `flag: true`, `log_level: warning`, `microsecond_noise: deterministic` (Q18)

    Also preserve useful existing config sections: `tags.test_commands`, `tags.lint_commands`, `tags.build_commands`, `tags.git_commands`, `risk.risky_commands` (rename to match new structure), `risk.protected_paths`, `reaction_keywords` (restructure into classification section), `mode_inference`, `gate_patterns`.

    Create `src/pipeline/__init__.py` and `src/pipeline/models/__init__.py` as empty init files for package structure.
  </action>
  <verify>
    Run: `python -c "import yaml; c = yaml.safe_load(open('data/config.yaml')); print(c['episode_timeout_seconds'], c['risk_model']['threshold'], c['classification']['min_confidence'])"`
    Expected output: `30 0.7 0.5`

    Run: `python -c "import yaml; c = yaml.safe_load(open('data/config.yaml')); print(len(c['classification']['reaction_keywords']['O_CORR']))"`
    Expected: Should show count of O_CORR keywords (>=5)
  </verify>
  <done>
    data/config.yaml contains ALL locked decision values from the 21 clarification answers. File is valid YAML with correct structure, sections, and values. Package __init__.py files exist.
  </done>
</task>

<task type="auto">
  <name>Task 1b: Create Pydantic config models and load_config()</name>
  <files>
    src/pipeline/models/config.py
  </files>
  <action>
    **Create `src/pipeline/models/config.py`** with Pydantic v2 models that validate the config.yaml structure from Task 1a:

    - `TemporalConfig(BaseModel)`: causal_window_seconds, link_confidence dict
    - `RiskModelConfig(BaseModel)`: threshold, combination_mode, risky_tools list, protected_paths list, false_positive_tolerance, acceptable_false_positive_rate
    - `ClassificationConfig(BaseModel)`: labels dict, reaction_keywords dict, min_confidence
    - `PayloadConfig(BaseModel)`: common_fields dict
    - `ValidationConfig(BaseModel)`: mode (strict|permissive), invalid_event_abort_threshold, abort_scope
    - `DeduplicationConfig(BaseModel)`: track_metadata, log_duplicates, duplicate_alert_threshold
    - `TemporalAnomalyConfig(BaseModel)`: tolerate, flag, log_level, microsecond_noise
    - `TagPatterns(BaseModel)`: test_commands, lint_commands, build_commands, git_commands, risky_commands
    - `PipelineConfig(BaseModel)`: episode_timeout_seconds, classification, risk_model, payload, temporal, deduplication, validation, temporal_anomalies, tags

    Add `load_config(path: Path = Path("data/config.yaml")) -> PipelineConfig` function that reads YAML via PyYAML and validates with Pydantic. Raise clear error if config file missing or invalid.

    Use Pydantic v2 patterns (model_config, @field_validator, not v1 @validator or class Config).
  </action>
  <verify>
    Run: `python -c "from src.pipeline.models.config import load_config; c = load_config(); print(c.episode_timeout_seconds, c.risk_model.threshold, c.classification.min_confidence)"`
    Expected output: `30 0.7 0.5`

    Run: `python -c "from src.pipeline.models.config import load_config, PipelineConfig; print(PipelineConfig.__fields__.keys())"`
    Expected: Shows all top-level config fields.

    Run: `python -c "from src.pipeline.models.config import load_config; load_config('nonexistent.yaml')"` should raise a clear FileNotFoundError or similar.
  </verify>
  <done>
    PipelineConfig Pydantic model loads and validates data/config.yaml correctly. All 9 sub-models exist with proper field types. load_config() reads YAML and returns validated PipelineConfig. Invalid config raises clear Pydantic validation errors. Missing file raises clear error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Pydantic data models for events and segments</name>
  <files>
    src/pipeline/models/events.py
    src/pipeline/models/segments.py
  </files>
  <action>
    **Create `src/pipeline/models/events.py`** with frozen Pydantic v2 models:

    `Classification(BaseModel, frozen=True)`:
    - label: str (O_DIR | O_GATE | O_CORR | X_PROPOSE | X_ASK | T_TEST | T_LINT | T_GIT_COMMIT | T_RISKY)
    - confidence: float (0.0-1.0, @field_validator ensures >= 0.0 and <= 1.0)
    - source: str (direct | inferred | risk_model)

    `CanonicalEvent(BaseModel, frozen=True)`:
    - event_id: str (deterministic hash, 16 hex chars)
    - ts_utc: datetime (timezone-aware UTC)
    - session_id: str
    - actor: str (human_orchestrator | executor | tool | system)
    - event_type: str (user_msg | assistant_text | assistant_thinking | tool_use | tool_result | git_commit | system_event)
    - payload: dict (with common + details structure per Q7)
    - links: dict (parent_uuid, tool_use_id, commit_hash, source_tool_uuid, etc.)
    - source_system: str (claude_jsonl | git)
    - source_ref: str (file:line_number or commit_hash)
    - risk_score: float = 0.0 (continuous 0.0-1.0)
    - risk_factors: list[dict] = [] (each: {factor, weight, matched})

    Add classmethod `make_event_id(source_system, session_id, turn_id, ts_utc, actor, event_type) -> str` using hashlib.sha256 truncated to 16 hex chars. This implements locked decision Q14.

    `TaggedEvent(BaseModel, frozen=True)`:
    - event: CanonicalEvent
    - primary: Classification | None (highest confidence wins per Q9)
    - secondaries: list[Classification] = [] (additive secondary labels)
    - all_classifications: list[Classification] = [] (all candidates for metadata per Q9)

    **Create `src/pipeline/models/segments.py`**:

    `EpisodeSegment(BaseModel)` (NOT frozen -- needs mutation during construction):
    - segment_id: str
    - session_id: str
    - start_event_id: str
    - end_event_id: str | None = None
    - start_ts: datetime
    - end_ts: datetime | None = None
    - start_trigger: str (O_DIR | O_GATE)
    - end_trigger: str | None = None (X_PROPOSE | X_ASK | T_TEST | T_RISKY | T_GIT_COMMIT | timeout | superseded | stream_end)
    - outcome: str | None = None (success | failure | timeout | superseded | stream_end)
    - events: list[str] = [] (list of event_ids)
    - event_count: int = 0
    - complexity: str = "simple" (simple | complex per Q3)
    - interruption_count: int = 0 (per Q3)
    - context_switches: int = 0 (per Q3)
    - config_hash: str | None = None

    Add methods: `add_event(event_id: str)`, `close(end_ts, end_event_id, end_trigger, outcome)`.

    All models must use Pydantic v2 patterns. Import datetime from datetime module. Use timezone-aware datetimes.
  </action>
  <verify>
    Run: `python -c "
from src.pipeline.models.events import CanonicalEvent, TaggedEvent, Classification
eid = CanonicalEvent.make_event_id('claude_jsonl', 'session1', 'turn1', '2026-01-01T00:00:00Z', 'executor', 'tool_use')
print('event_id length:', len(eid))
print('hex chars:', all(c in '0123456789abcdef' for c in eid))
# Verify deterministic: same input -> same output
eid2 = CanonicalEvent.make_event_id('claude_jsonl', 'session1', 'turn1', '2026-01-01T00:00:00Z', 'executor', 'tool_use')
print('deterministic:', eid == eid2)
"`
    Expected: event_id length: 16, hex chars: True, deterministic: True

    Run: `python -c "
from src.pipeline.models.segments import EpisodeSegment
from datetime import datetime, timezone
seg = EpisodeSegment(segment_id='seg1', session_id='s1', start_event_id='e1', start_ts=datetime.now(timezone.utc), start_trigger='O_DIR')
seg.add_event('e1')
seg.add_event('e2')
print('event_count:', seg.event_count)
print('events:', seg.events)
"`
    Expected: event_count: 2, events: ['e1', 'e2']
  </verify>
  <done>
    All Pydantic models validate correctly. CanonicalEvent.make_event_id produces deterministic 16-char hex hashes. EpisodeSegment supports mutable construction with add_event() and close(). Frozen models reject mutation attempts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create DuckDB schema and connection management</name>
  <files>
    src/pipeline/storage/__init__.py
    src/pipeline/storage/schema.py
    requirements.txt
  </files>
  <action>
    **Create `src/pipeline/storage/__init__.py`** as empty init file.

    **Create `src/pipeline/storage/schema.py`** with:

    `get_connection(db_path: str = "data/ope.db") -> duckdb.DuckDBPyConnection`:
    Returns a DuckDB connection. Creates parent directories if needed.

    `create_schema(conn: duckdb.DuckDBPyConnection) -> None`:
    Creates the events and episode_segments tables using CREATE TABLE IF NOT EXISTS. Schema from research doc:

    ```sql
    CREATE TABLE IF NOT EXISTS events (
        event_id VARCHAR PRIMARY KEY,
        ts_utc TIMESTAMPTZ NOT NULL,
        session_id VARCHAR NOT NULL,
        actor VARCHAR NOT NULL,
        event_type VARCHAR NOT NULL,
        primary_tag VARCHAR,
        primary_tag_confidence FLOAT,
        secondary_tags JSON,
        payload JSON,
        links JSON,
        risk_score FLOAT DEFAULT 0.0,
        risk_factors JSON,
        first_seen TIMESTAMPTZ DEFAULT current_timestamp,
        last_seen TIMESTAMPTZ DEFAULT current_timestamp,
        ingestion_count INTEGER DEFAULT 1,
        source_system VARCHAR NOT NULL,
        source_ref VARCHAR NOT NULL
    );

    CREATE TABLE IF NOT EXISTS episode_segments (
        segment_id VARCHAR PRIMARY KEY,
        session_id VARCHAR NOT NULL,
        start_event_id VARCHAR NOT NULL,
        end_event_id VARCHAR,
        start_ts TIMESTAMPTZ NOT NULL,
        end_ts TIMESTAMPTZ,
        start_trigger VARCHAR NOT NULL,
        end_trigger VARCHAR,
        outcome VARCHAR,
        event_count INTEGER NOT NULL,
        event_ids JSON NOT NULL,
        complexity VARCHAR DEFAULT 'simple',
        interruption_count INTEGER DEFAULT 0,
        context_switches INTEGER DEFAULT 0,
        config_hash VARCHAR,
        created_at TIMESTAMPTZ DEFAULT current_timestamp
    );
    ```

    Create indexes: idx_events_session, idx_events_tag, idx_events_ts, idx_segments_session.

    Add a `drop_schema(conn)` function for testing (drops tables if exist).

    **Update `requirements.txt`**: Add `loguru>=0.7` to active dependencies. Remove the outdated `jsonlines` dependency. Ensure `duckdb`, `pydantic`, `pyyaml`, `jsonschema`, `click`, `tqdm` are in the active (uncommented) section. Add `pytest>=8.0` to dev dependencies (uncommented). Note: Do NOT add orjson -- we are using DuckDB for JSONL reading per user decision.
  </action>
  <verify>
    Run: `python -c "
from src.pipeline.storage.schema import get_connection, create_schema
conn = get_connection(':memory:')
create_schema(conn)
tables = conn.execute(\"SELECT table_name FROM information_schema.tables WHERE table_schema='main'\").fetchall()
print('Tables:', sorted([t[0] for t in tables]))
cols = conn.execute(\"SELECT column_name FROM information_schema.columns WHERE table_name='events'\").fetchall()
print('Event columns:', len(cols))
"`
    Expected: Tables: ['episode_segments', 'events'], Event columns: 17
  </verify>
  <done>
    DuckDB schema creates correctly in memory and on disk. Events table has all 17 columns matching the research spec. Episode_segments table has all 15 columns. Indexes are created. requirements.txt is updated with correct dependencies (no orjson, added loguru).
  </done>
</task>

</tasks>

<verification>
1. `python -c "from src.pipeline.models.config import load_config; c = load_config(); print('Config OK:', c.episode_timeout_seconds == 30)"` prints "Config OK: True"
2. `python -c "from src.pipeline.models.events import CanonicalEvent, TaggedEvent; print('Models OK')"` prints "Models OK"
3. `python -c "from src.pipeline.storage.schema import get_connection, create_schema; c = get_connection(':memory:'); create_schema(c); print('Schema OK')"` prints "Schema OK"
4. All 21 locked decision values are present in data/config.yaml (spot check: episode_timeout_seconds=30, risk threshold=0.7, O_CORR keywords, X_PROPOSE definition)
</verification>

<success_criteria>
- Config loads from YAML with all locked decision values
- Pydantic models validate data and enforce immutability where specified
- DuckDB schema creates both tables with correct column types
- make_event_id produces deterministic, idempotent hashes
- No orjson dependency (DuckDB handles JSONL reading per user decision)
- requirements.txt is updated and consistent
</success_criteria>

<output>
After completion, create `.planning/phases/01-event-stream-foundation/01-01-SUMMARY.md`
</output>
