---
phase: 25-genus-protocol-propagation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/CLAUDE.md
  - src/pipeline/live/bus/models.py
  - src/pipeline/live/governor/daemon.py
  - src/pipeline/live/bus/server.py
  - src/pipeline/live/hooks/session_start.py
  - src/pipeline/live/governor/briefing.py
  - .planning/REQUIREMENTS.md
autonomous: true

must_haves:
  truths:
    - "CLAUDE.md Premise Declaration Protocol section includes optional GENUS line format after SCOPE"
    - "CLAUDE.md has Genus Declaration subsection with fundamentality criterion and mechanism-vs-symptom distinction"
    - "CheckResponse model has genus_count field with default 0"
    - "GovernorDaemon.get_briefing() queries axis_edges for genus_of edge count scoped to requesting repo"
    - "/api/check response JSON includes genus_count integer"
    - "session_start.py emits genus hint when genus_count > 0, silent when 0"
    - "REQUIREMENTS.md has GENEXT-01, GENEXT-02, GENORACLE-01, GENORACLE-02, GENORACLE-03, REFRAME-01"
  artifacts:
    - path: "~/.claude/CLAUDE.md"
      provides: "GENUS field format in Premise Declaration Protocol"
      contains: "GENUS:"
    - path: "src/pipeline/live/bus/models.py"
      provides: "CheckResponse with genus_count field"
      contains: "genus_count"
    - path: "src/pipeline/live/governor/daemon.py"
      provides: "Genus count query in get_briefing"
      contains: "genus_count"
    - path: "src/pipeline/live/hooks/session_start.py"
      provides: "Genus hint output in session start"
      contains: "genus_count"
  key_links:
    - from: "src/pipeline/live/governor/daemon.py"
      to: "axis_edges table"
      via: "DuckDB SELECT COUNT for genus_of edges with repo JOIN"
      pattern: "genus_of"
    - from: "src/pipeline/live/bus/server.py"
      to: "src/pipeline/live/bus/models.py"
      via: "genus_count field in check response JSON"
      pattern: "genus_count"
    - from: "src/pipeline/live/hooks/session_start.py"
      to: "/api/check response"
      via: "check.get('genus_count', 0)"
      pattern: "genus_count"
---

<objective>
Extend the global CLAUDE.md with the GENUS field format in the Premise Declaration Protocol, extend GovernorDaemon and CheckResponse to include genus_count in the /api/check response, and add genus hint output to session_start.py. Also add Phase 25 requirements to REQUIREMENTS.md.

Purpose: Wave 1 establishes the documentation contract (CLAUDE.md) and the passive advisory channel (session_start genus hint). These are the two outward-facing deliverables that make genus-first behavior visible to every session without requiring active invocation.
Output: Updated CLAUDE.md, updated CheckResponse/GovernorDaemon/server/session_start, updated REQUIREMENTS.md.
</objective>

<execution_context>
@/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/david/projects/orchestrator-policy-extraction/.planning/PROJECT.md
@/Users/david/projects/orchestrator-policy-extraction/.planning/ROADMAP.md
@/Users/david/projects/orchestrator-policy-extraction/.planning/STATE.md
@/Users/david/projects/orchestrator-policy-extraction/.planning/phases/25-genus-protocol-propagation/25-RESEARCH.md
@/Users/david/.claude/CLAUDE.md
@/Users/david/projects/orchestrator-policy-extraction/src/pipeline/live/bus/models.py
@/Users/david/projects/orchestrator-policy-extraction/src/pipeline/live/governor/daemon.py
@/Users/david/projects/orchestrator-policy-extraction/src/pipeline/live/bus/server.py
@/Users/david/projects/orchestrator-policy-extraction/src/pipeline/live/hooks/session_start.py
@/Users/david/projects/orchestrator-policy-extraction/.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CLAUDE.md with GENUS field format and Genus Declaration subsection</name>
  <files>
    ~/.claude/CLAUDE.md
  </files>
  <action>
Read `~/.claude/CLAUDE.md` in full. Make additive-only changes:

1. In the `### Declaration Format` subsection, add a 5th line to the PREMISE block format inside the code fence, right after the `SCOPE:` line:

```
GENUS: [mechanism name] | INSTANCES: [instance A, instance B]
```

2. After the existing code fence for the declaration format, add a brief note:

```
The GENUS line is optional. Include it when your write addresses a problem
that has a nameable mechanism — what process, when absent, causes the failure.
```

3. Add a new subsection `### Genus Declaration` AFTER the existing `### Staleness Rule` subsection (and BEFORE `### Foil Verification Format`). Contents:

```markdown
### Genus Declaration

The GENUS field declares the fundamental mechanism behind a problem before
implementing a fix. A valid genus satisfies the fundamentality criterion:

1. **Two citable instances** -- at least 2 specific, named occurrences where the
   same mechanism produced the same failure class
2. **Causal explanation** -- the name encodes a mechanism (what process, when
   absent or violated, produces the failure), not a symptom (the failure itself)

**Mechanism vs. symptom test:** does the genus name imply the solution structure?
If the solution's passes are readable from the genus name, it is a mechanism.
If additional creative leaps are needed, the genus is probably still a symptom.

| Symptom (invalid) | Mechanism (valid) |
|-------------------|-------------------|
| "search failure"  | "corpus-relative identity retrieval" |
| "import error"    | "module boundary dissolution" |
| "state corruption" | "non-atomic write propagation" |
```

CRITICAL: Do NOT modify existing PREMISE, VALIDATED_BY, FOIL, or SCOPE field definitions. Do NOT modify the existing `### The Test` subsection, `### Foil Verification Format`, or `### Why This Exists`. This is additive only.
  </action>
  <verify>
Read `~/.claude/CLAUDE.md` and confirm:
1. The declaration format code fence now has 5 lines (PREMISE, VALIDATED_BY, FOIL, SCOPE, GENUS)
2. A `### Genus Declaration` subsection exists with the fundamentality criterion
3. All original subsections still exist unchanged: `### Declaration Format`, `### Staleness Rule`, `### Foil Verification Format`, `### The Test`, `### Why This Exists`
  </verify>
  <done>
CLAUDE.md Premise Declaration Protocol section includes the optional GENUS line format and a Genus Declaration subsection with fundamentality criterion, mechanism-vs-symptom distinction, and example table. No existing content modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend CheckResponse + GovernorDaemon + server + session_start for genus_count</name>
  <files>
    src/pipeline/live/bus/models.py
    src/pipeline/live/governor/daemon.py
    src/pipeline/live/bus/server.py
    src/pipeline/live/hooks/session_start.py
  </files>
  <action>
**In `src/pipeline/live/bus/models.py`:**
Add `genus_count: int = 0` field to the `CheckResponse` model, after `relevant_docs`.

**In `src/pipeline/live/governor/daemon.py`:**
1. Add a new method `_query_genus_count(self, repo: str | None = None) -> int` that:
   - Opens a DuckDB connection to `self._db_path` (same pattern as `_query_relevant_docs`)
   - Checks if `axis_edges` table exists via `information_schema.tables` (fail-open: return 0 if missing)
   - If `repo` is provided, also checks if `bus_sessions` table exists for the repo-scoped JOIN
   - When both tables exist and repo is provided:
     ```sql
     SELECT COUNT(DISTINCT ae.axis_a) FROM axis_edges ae
     JOIN bus_sessions bs ON ae.created_session_id = bs.session_id
     WHERE ae.relationship_text = 'genus_of'
     AND ae.status IN ('candidate', 'active')
     AND bs.repo = ?
     ```
   - When repo is None (or bus_sessions missing):
     ```sql
     SELECT COUNT(DISTINCT axis_a) FROM axis_edges
     WHERE relationship_text = 'genus_of'
     AND status IN ('candidate', 'active')
     ```
   - Wraps everything in try/except, returns 0 on any error (fail-open)
   - Closes connection in finally block

2. In `get_briefing()`, after `relevant_docs = self._query_relevant_docs()`, add:
   ```python
   genus_count = self._query_genus_count(repo=repo)
   ```
   And update the return to include genus_count:
   ```python
   return briefing.model_copy(update={"relevant_docs": relevant_docs, "genus_count": genus_count})
   ```

3. Add `genus_count: int = 0` to `ConstraintBriefing` in `briefing.py` (the model returned by `get_briefing`).

**In `src/pipeline/live/bus/server.py`:**
In the `check()` handler, add `"genus_count": briefing.genus_count` to the success JSONResponse dict (alongside constraints, interventions, epistemological_signals, relevant_docs). Also add `"genus_count": 0` to the fail-open default response.

**In `src/pipeline/live/hooks/session_start.py`:**
After the `relevant_docs` section (after line 130), add:

```python
# Genus hint (Phase 25)
genus_count = check.get("genus_count", 0)
if genus_count > 0:
    print(
        f"\n[OPE] GENUS: {genus_count} prior genera available "
        f"-- /genus-first before writing",
        flush=True,
    )
# else: no genera -- silent
```

Follow the same pattern as the relevant_docs section: silent when count is 0, `[OPE]` prefix when non-zero.
  </action>
  <verify>
Run: `python -m pytest tests/test_bus_foundation.py tests/test_bus_integration.py -v`

All existing bus tests pass. Confirm:
1. `from src.pipeline.live.bus.models import CheckResponse; r = CheckResponse(); assert r.genus_count == 0`
2. `from src.pipeline.live.governor.briefing import ConstraintBriefing; b = ConstraintBriefing(); assert b.genus_count == 0`
  </verify>
  <done>
CheckResponse has genus_count field defaulting to 0. GovernorDaemon queries axis_edges for genus_of count (repo-scoped when repo provided). /api/check includes genus_count in response JSON. session_start.py emits genus hint line when genus_count > 0 and is silent when 0. All existing bus tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 25 requirements to REQUIREMENTS.md</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <action>
Read `.planning/REQUIREMENTS.md`. Add a new section before `## Traceability`:

```markdown
### Genus Protocol Propagation

- [ ] **GENEXT-01**: System extends `~/.claude/CLAUDE.md` Premise Declaration Protocol with optional GENUS field format (5th line: `GENUS: [name] | INSTANCES: [A, B]`), fundamentality criterion (>=2 instances + causal explanation), and mechanism-vs-symptom distinction -- additive only, no changes to existing fields
- [ ] **GENEXT-02**: System emits `[OPE] GENUS: N prior genera available -- /genus-first before writing` at session start when axis_edges has >=1 genus_of edge scoped to the current session's repo; silent when 0
- [ ] **GENORACLE-01**: System provides `POST /api/genus-consult` bus endpoint accepting `{problem, session_id, repo}` and returning `{genus, instances, valid, confidence}` within 500ms using keyword-overlap scoring against axis_edges genus_of entries
- [ ] **GENORACLE-02**: System implements GenusOracleHandler with tokenization-based search over axis_edges (adapted from doc_query.py pattern), confidence scoring as matched_tokens/total_genus_tokens, instance extraction from evidence JSON, and repo scoping via bus_sessions JOIN
- [ ] **GENORACLE-03**: System extends `/genus-first` SKILL.md with Mode A bus path: when data/ope.db absent AND OPE_BUS_SOCKET set, POST to /api/genus-consult for genus grounding; result embedded in genus-first output prompt
- [ ] **REFRAME-01**: System provides global `/reframe` skill at `~/.claude/skills/reframe/` that detects capability tier (OPE-local / OPE-via-bus / Lightweight), selects a reasoning protocol (Premise-Declare-First / Axis-Before-Fix / Pattern-Not-Symptom) given any problem, uses genus oracle for grounding when bus reachable, emits verbatim protocol directive
```

In the `## Traceability` table, add:

```
| GENEXT-01 | Phase 25: Genus Protocol Propagation | Pending |
| GENEXT-02 | Phase 25: Genus Protocol Propagation | Pending |
| GENORACLE-01 | Phase 25: Genus Protocol Propagation | Pending |
| GENORACLE-02 | Phase 25: Genus Protocol Propagation | Pending |
| GENORACLE-03 | Phase 25: Genus Protocol Propagation | Pending |
| REFRAME-01 | Phase 25: Genus Protocol Propagation | Pending |
```

Update the Coverage section at the bottom:
```
- Genus protocol propagation requirements: 6 (GENEXT-01, GENEXT-02, GENORACLE-01 through GENORACLE-03, REFRAME-01, Phase 25)
- Mapped to phases: 39
```
  </action>
  <verify>
Read `.planning/REQUIREMENTS.md` and confirm GENEXT-01, GENEXT-02, GENORACLE-01, GENORACLE-02, GENORACLE-03, and REFRAME-01 appear in both the requirements section and the traceability table.
  </verify>
  <done>
REQUIREMENTS.md has all 6 Phase 25 requirements defined and traced. Coverage section updated.
  </done>
</task>

</tasks>

<verification>
Full Wave 1 verification:
1. `python -m pytest tests/test_bus_foundation.py tests/test_bus_integration.py -v` -- all existing bus tests pass
2. `python -c "from src.pipeline.live.bus.models import CheckResponse; r = CheckResponse(); assert r.genus_count == 0; print('OK')"` -- prints OK
3. `python -c "from src.pipeline.live.governor.briefing import ConstraintBriefing; b = ConstraintBriefing(); assert b.genus_count == 0; print('OK')"` -- prints OK
4. `grep -c 'GENUS:' ~/.claude/CLAUDE.md` -- returns at least 2 (declaration format + genus declaration subsection)
5. `grep 'GENEXT-01' .planning/REQUIREMENTS.md` -- returns a match
</verification>

<success_criteria>
- CLAUDE.md has GENUS field in declaration format and Genus Declaration subsection
- CheckResponse.genus_count field exists with default 0
- GovernorDaemon queries axis_edges for genus_of count
- /api/check includes genus_count in response
- session_start.py emits genus hint when count > 0
- REQUIREMENTS.md has all 6 Phase 25 requirements
- All existing bus tests pass
</success_criteria>

<output>
After completion, create `/Users/david/projects/orchestrator-policy-extraction/.planning/phases/25-genus-protocol-propagation/25-01-SUMMARY.md`
</output>
