---
phase: 14-live-session-governance-research
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - .planning/phases/14-live-session-governance-research/14-03-BLUEPRINT.md
autonomous: true

must_haves:
  truths:
    - "Phase 15 implementation plan exists with 5 executable waves, each containing specific file targets and task descriptions"
    - "Every API contract from 14-01 and 14-02 is mapped to a concrete source file with class/function signatures"
    - "Test strategy is defined for each wave with specific test file paths and test categories (unit, integration, hook protocol)"
    - "Dependency chain between waves is explicit: Wave N outputs are Wave N+1 inputs"
    - "Hook registration in .claude/settings.json is specified as the final integration step"
  artifacts:
    - path: ".planning/phases/14-live-session-governance-research/14-03-BLUEPRINT.md"
      provides: "Complete Phase 15 implementation blueprint with 5 waves, file targets, API contracts, and test strategies"
      contains: "Wave 1"
  key_links:
    - from: "14-03-BLUEPRINT.md Wave 1 file targets"
      to: "14-01-DESIGN.md hook contracts"
      via: "Wave 1 implements the hook scripts specified in 14-01"
      pattern: "governance_check.py, constraint_briefing.py"
    - from: "14-03-BLUEPRINT.md Wave 3 file targets"
      to: "14-02-DESIGN.md bus API routes"
      via: "Wave 3 implements the bus server specified in 14-02"
      pattern: "bus/server.py, bus/client.py"
    - from: "14-03-BLUEPRINT.md Wave 4 file targets"
      to: "14-02-DESIGN.md governing session"
      via: "Wave 4 implements the governor daemon specified in 14-02"
      pattern: "governor/monitor.py"
---

<objective>
Synthesize the designs from 14-01 (hooks + stream processor) and 14-02 (bus + governing session) into a concrete Phase 15 implementation blueprint with executable waves, specific file targets, API contracts, and test strategies.

Purpose: Phase 15 implementers need a detailed roadmap that maps design specifications to actual code files, defines the implementation order, specifies test coverage requirements, and provides enough detail to create GSD-style PLAN.md files directly from this blueprint. This is the bridge from research to execution.

Output: A single blueprint document (`14-03-BLUEPRINT.md`) that serves as the planning input for Phase 15's `/gsd:plan-phase`.
</objective>

<execution_context>
@/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-live-session-governance-research/14-RESEARCH.md

# Design documents from this phase (must be read during execution)
@.planning/phases/14-live-session-governance-research/14-01-SUMMARY.md
@.planning/phases/14-live-session-governance-research/14-02-SUMMARY.md

# Existing pipeline structure for reference
@src/pipeline/feedback/checker.py
@src/pipeline/constraint_store.py
@src/pipeline/escalation/detector.py
@src/pipeline/durability/amnesia.py
@src/pipeline/cli/__main__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Phase 15 wave structure and file targets</name>
  <files>.planning/phases/14-live-session-governance-research/14-03-BLUEPRINT.md</files>
  <action>
Create the blueprint document. Read the 14-01-SUMMARY.md and 14-02-SUMMARY.md to understand what was designed, then write the first major section: Phase 15 Wave Structure.

The blueprint must start with a Phase 15 overview:

**0.1 Phase 15 Overview**
- Phase name: "Live Session Governance Implementation"
- Goal: Implement the live governance layer designed in Phase 14 -- hook scripts, stream processor, coordination bus, and governing session pattern.
- Depends on: Phase 14 (design complete), Phase 13 (PolicyViolationChecker and constraint store ready)
- Requirements: LIVE-01 through LIVE-05 (same as Phase 14, now implemented)
- Estimated plans: 8-10 plans across 5 waves
- New dependency: `pip install watchdog>=6.0.0`

**0.2 Module Structure**
Define the exact directory and file structure for `src/pipeline/live/`:
```
src/pipeline/live/
  __init__.py
  hooks/
    __init__.py
    governance_check.py    # PreToolUse hook handler (LIVE-01)
    constraint_briefing.py # SessionStart hook handler (LIVE-02)
    log_event.py           # PostToolUse async event logger
    models.py              # HookInput, GovernanceDecision Pydantic models
  stream/
    __init__.py
    processor.py           # StreamProcessor class with watchdog
    adapters.py            # LiveEventAdapter, LiveEvent dataclass
    incremental.py         # IncrementalEscalationAdapter, IncrementalAmnesiaAdapter
  bus/
    __init__.py
    server.py              # Starlette ASGI app + routes
    client.py              # BusClient (sync + async httpx wrappers)
    models.py              # Bus request/response Pydantic models
    state.py               # SharedState, SessionRecord, ProjectState
  governor/
    __init__.py
    daemon.py              # GovernorDaemon class (async tasks on bus event loop)
    decisions.py           # Decision matrix logic
    broadcaster.py         # Broadcast message delivery
```
For each file, provide a one-line description of its purpose. Include the corresponding test file path in `tests/pipeline/live/`.

Then define each wave:

**Wave 1: Core Hook Scripts (LIVE-01 + LIVE-02)**
- Scope: Standalone hook scripts that work without bus. Direct file-based constraint loading.
- Files created:
  - `src/pipeline/live/__init__.py`
  - `src/pipeline/live/hooks/__init__.py`
  - `src/pipeline/live/hooks/models.py` -- HookInput, GovernanceDecision Pydantic models
  - `src/pipeline/live/hooks/governance_check.py` -- PreToolUse handler with decision logic
  - `src/pipeline/live/hooks/constraint_briefing.py` -- SessionStart handler with briefing generation
  - `tests/pipeline/live/hooks/test_governance_check.py`
  - `tests/pipeline/live/hooks/test_constraint_briefing.py`
  - `tests/pipeline/live/hooks/test_models.py`
- API contracts: list the exact function signatures and class interfaces from the 14-01 design
- Dependencies: existing ConstraintStore, PolicyViolationChecker, scopes_overlap()
- Estimated plans: 2 (one for models + governance_check, one for constraint_briefing)
- Success criteria: hooks work via `echo '{"tool_name":"Bash",...}' | python3 governance_check.py` with correct JSON output

**Wave 2: JSONL Stream Processor (LIVE-03)**
- Scope: Background daemon that tails session JSONL files and runs detectors.
- Files created:
  - `src/pipeline/live/stream/__init__.py`
  - `src/pipeline/live/stream/adapters.py` -- LiveEvent, LiveEventAdapter
  - `src/pipeline/live/stream/incremental.py` -- IncrementalEscalationAdapter, IncrementalAmnesiaAdapter, IncrementalPolicyCheckAdapter
  - `src/pipeline/live/stream/processor.py` -- StreamProcessor with watchdog Observer
  - `tests/pipeline/live/stream/test_adapters.py`
  - `tests/pipeline/live/stream/test_incremental.py`
  - `tests/pipeline/live/stream/test_processor.py`
- API contracts: list class interfaces from the 14-01 design section 2
- Dependencies: Wave 1 models, existing EscalationDetector, AmnesiaDetector, PolicyViolationChecker; NEW: watchdog
- Estimated plans: 2 (one for adapters + incremental, one for processor)
- Success criteria: processor detects events appended to a test JSONL file within 200ms

**Wave 3: Inter-Session Bus (LIVE-04)**
- Scope: HTTP service on Unix socket providing shared state across sessions.
- Files created:
  - `src/pipeline/live/bus/__init__.py`
  - `src/pipeline/live/bus/models.py` -- Bus request/response Pydantic models
  - `src/pipeline/live/bus/state.py` -- SharedState, SessionRecord, ProjectState
  - `src/pipeline/live/bus/server.py` -- Starlette app with all routes from 14-02
  - `src/pipeline/live/bus/client.py` -- BusClient wrapper
  - `tests/pipeline/live/bus/test_models.py`
  - `tests/pipeline/live/bus/test_state.py`
  - `tests/pipeline/live/bus/test_server.py`
  - `tests/pipeline/live/bus/test_client.py`
- API contracts: list all 10 route handler signatures from the 14-02 design
- Dependencies: Wave 1 models (GovernanceDecision, GovernanceSignal), existing ConstraintStore, PolicyViolationChecker
- Estimated plans: 2-3 (models+state, server+routes, client+integration)
- Success criteria: bus starts on Unix socket, responds to /api/health, /api/check returns correct decisions

**Wave 4: Governor Daemon (LIVE-05)**
- Scope: Async tasks integrated into bus process for monitoring and decision-making.
- Files created:
  - `src/pipeline/live/governor/__init__.py`
  - `src/pipeline/live/governor/daemon.py` -- GovernorDaemon async task runner
  - `src/pipeline/live/governor/decisions.py` -- Decision matrix implementation
  - `src/pipeline/live/governor/broadcaster.py` -- Broadcast delivery logic
  - `tests/pipeline/live/governor/test_daemon.py`
  - `tests/pipeline/live/governor/test_decisions.py`
  - `tests/pipeline/live/governor/test_broadcaster.py`
- API contracts: list class interfaces from the 14-02 design section 2
- Dependencies: Wave 3 bus (SharedState, server routes), Wave 2 stream processor signals
- Estimated plans: 2 (daemon+decisions, broadcaster+integration)
- Success criteria: governor detects mock escalation signal and broadcasts warning to registered session

**Wave 5: Integration + Hook Registration**
- Scope: Wire everything together, register hooks in .claude/settings.json, add CLI commands, end-to-end tests.
- Files created/modified:
  - `.claude/settings.json` -- Add PreToolUse and SessionStart hook configurations
  - `src/pipeline/cli/govern.py` -- Add `bus start/stop/status` and `live monitor` subcommands
  - `src/pipeline/live/hooks/governance_check.py` -- Add bus fallback (try bus, fall back to direct)
  - `src/pipeline/live/hooks/constraint_briefing.py` -- Add bus session registration
  - `tests/pipeline/live/test_integration.py` -- End-to-end hook + bus + governor tests
  - `tests/pipeline/live/test_hook_protocol.py` -- Validate exact JSON protocol compliance
- Dependencies: All previous waves
- Estimated plans: 1-2 (CLI + hook registration, integration tests)
- Success criteria: `python -m src.pipeline.cli govern bus start` starts the bus; hooks in .claude/settings.json fire correctly on tool use
  </action>
  <verify>
The blueprint document exists and contains:
1. Phase 15 overview with goal, dependencies, and plan estimate
2. Complete module structure with all file paths
3. 5 wave definitions, each with files created, API contracts, dependencies, plan estimates, and success criteria
4. At least 30 source files listed across all waves
5. At least 15 test files listed across all waves
  </verify>
  <done>
Phase 15 wave structure is defined with 5 waves, each containing specific file targets, dependencies, plan estimates, and success criteria. Every source file and test file for the live governance module is accounted for.
  </done>
</task>

<task type="auto">
  <name>Task 2: Specify API contracts, test strategy, and Phase 15 requirements</name>
  <files>.planning/phases/14-live-session-governance-research/14-03-BLUEPRINT.md</files>
  <action>
Append the remaining sections to the blueprint document. Read the 14-01-DESIGN.md and 14-02-DESIGN.md (the full design documents, not just summaries) to extract the exact API contracts and translate them into implementation specifications.

**3.1 API Contract Summary**
For each major class/function in the implementation, provide the exact Python signature with type hints and a one-line docstring. Group by wave:

Wave 1 contracts:
- `HookInput` model fields and validators
- `GovernanceDecision` model fields
- `governance_check.main()` -- reads stdin, returns stdout JSON
- `constraint_briefing.main()` -- reads stdin, returns stdout JSON
- `build_search_text(tool_name: str, tool_input: dict) -> str`
- `format_briefing(constraints: list[dict], durability_scores: dict[str, float], cwd: str) -> str`

Wave 2 contracts:
- `LiveEvent` dataclass fields
- `LiveEventAdapter.adapt(raw_event: dict) -> LiveEvent | None`
- `IncrementalEscalationAdapter.process_event(event: LiveEvent) -> list[EscalationCandidate]`
- `IncrementalAmnesiaAdapter.process_event(event: LiveEvent) -> list[GovernanceSignal]`
- `StreamProcessor.__init__(project_dir: str, bus_client: BusClient | None)`
- `StreamProcessor.start()` / `StreamProcessor.stop()`

Wave 3 contracts:
- All 10 route handler function signatures (async def health, async def check_constraint, etc.)
- `SharedState` class with add_session, remove_session, get_constraints, check_constraint, add_event methods
- `BusClient.__init__(socket_path: str)` / `BusClient.check(tool_name, tool_input) -> GovernanceDecision`
- `BusClient.is_available() -> bool` -- tests socket connectivity

Wave 4 contracts:
- `GovernorDaemon.__init__(state: SharedState)` / `GovernorDaemon.start()` / `GovernorDaemon.stop()`
- `decide(signal: GovernanceSignal) -> GovernanceAction` -- decision matrix function
- `broadcast(message: BroadcastMessage, targets: list[str] | Literal["all"]) -> BroadcastResult`

**3.2 Test Strategy**
For each wave, specify:
- Test categories: unit tests (isolated function behavior), integration tests (component interaction), protocol tests (JSON schema compliance)
- Specific test scenarios to cover (at least 5 per wave)
- Test infrastructure needed:
  - Wave 1: stdin/stdout capture for hook testing (subprocess or io.StringIO)
  - Wave 2: temporary JSONL files with controlled append timing for stream processor testing
  - Wave 3: test client against running bus (use httpx.AsyncClient with app=starlette_app for in-process testing, no actual socket needed)
  - Wave 4: mock bus state for governor decision testing
  - Wave 5: full integration with real Unix socket, real JSONL file, real hook subprocess
- Test count estimates per wave (guide for plan sizing):
  - Wave 1: ~25 tests (models: 8, governance_check: 10, constraint_briefing: 7)
  - Wave 2: ~30 tests (adapters: 10, incremental: 12, processor: 8)
  - Wave 3: ~35 tests (models: 5, state: 10, server: 12, client: 8)
  - Wave 4: ~20 tests (daemon: 5, decisions: 10, broadcaster: 5)
  - Wave 5: ~15 tests (integration: 10, hook protocol: 5)
  - Total: ~125 new tests

**3.3 Phase 15 Requirements (LIVE-01 through LIVE-05)**
Restate each requirement with specific acceptance criteria mapped to implementation:
- LIVE-01: governance_check.py reads hook JSON, runs PolicyViolationChecker, returns deny/warn/allow within 200ms. Test: `time echo '...' | python3 governance_check.py` completes in <200ms.
- LIVE-02: constraint_briefing.py loads active constraints filtered by cwd scope, ranks by durability, returns additionalContext briefing. Test: briefing includes CRITICAL section for low-durability constraints.
- LIVE-03: StreamProcessor tails JSONL, runs detectors on each event, emits GovernanceSignal within 200ms of event write. Test: append event to JSONL file, assert signal emitted within 200ms.
- LIVE-04: Bus serves 10 API routes on Unix socket, caches constraints in memory, registers/deregisters sessions. Test: start bus, POST /api/check, verify correct decision.
- LIVE-05: GovernorDaemon monitors bus events, applies decision matrix, broadcasts blocks/warnings. Test: emit escalation signal, verify broadcast delivered to registered session.

**3.4 Phase 15 Success Criteria**
Define the overall phase completion criteria:
1. All 5 requirements (LIVE-01 through LIVE-05) have passing acceptance tests
2. ~125 new tests passing with zero regressions on existing ~889 tests
3. `python -m src.pipeline.cli govern bus start` starts the governance bus
4. `.claude/settings.json` contains PreToolUse and SessionStart hook configurations
5. Single-session governance works without bus (standalone mode)
6. Multi-session governance works with bus (coordinated mode)
7. End-to-end test: start bus -> register session -> check constraint -> detect escalation -> broadcast warning

**3.5 Risks and Mitigations**
- Risk: Python startup time exceeds 200ms budget for PreToolUse hook -> Mitigation: measure early in Wave 1; if too slow, implement bus-first path or compiled shim in Wave 5
- Risk: watchdog FSEvents behavior differs from expectations -> Mitigation: integration test in Wave 2 with real file appends; fallback to polling if FSEvents unreliable
- Risk: httpx Unix socket API differs from training data -> Mitigation: verify API in Wave 3 first test; adapt transport configuration if needed
- Risk: starlette SSE implementation for /api/events/stream is complex -> Mitigation: start with polling-based event consumption; upgrade to SSE in Wave 5 if needed
- Risk: governor daemon async task management conflicts with bus request handling -> Mitigation: use asyncio.create_task with proper cancellation; test concurrent request + monitoring scenarios

**3.6 Phase 15 Plan Mapping**
Map waves to estimated GSD plans (to guide `/gsd:plan-phase` for Phase 15):
- Plan 15-01: Wave 1a -- Hook models + governance_check.py (2 tasks, ~30min)
- Plan 15-02: Wave 1b -- constraint_briefing.py + hook protocol tests (2 tasks, ~30min)
- Plan 15-03: Wave 2a -- LiveEventAdapter + incremental detector adapters (2 tasks, ~40min)
- Plan 15-04: Wave 2b -- StreamProcessor with watchdog (2 tasks, ~40min)
- Plan 15-05: Wave 3a -- Bus models + SharedState + server routes (3 tasks, ~45min)
- Plan 15-06: Wave 3b -- BusClient + bus integration tests (2 tasks, ~30min)
- Plan 15-07: Wave 4 -- Governor daemon + decisions + broadcaster (2-3 tasks, ~45min)
- Plan 15-08: Wave 5a -- CLI commands + hook registration (2 tasks, ~30min)
- Plan 15-09: Wave 5b -- End-to-end integration tests (2 tasks, ~30min)
Total: 9 plans, ~5.5 hours estimated execution time
  </action>
  <verify>
The blueprint document now also contains:
1. Section 3.1 with at least 15 function/class signatures with type hints
2. Section 3.2 with test strategy per wave and test count estimates totaling ~125
3. Section 3.3 with all 5 LIVE requirements mapped to acceptance criteria
4. Section 3.4 with at least 5 success criteria for Phase 15
5. Section 3.5 with at least 4 risks and mitigations
6. Section 3.6 with at least 8 plan mappings for Phase 15
  </verify>
  <done>
Phase 15 implementation blueprint is complete with API contracts, test strategy, requirement acceptance criteria, success criteria, risks/mitigations, and plan mapping. The blueprint is detailed enough to serve as direct input to `/gsd:plan-phase` for Phase 15.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the blueprint covers:
- [ ] Phase 15 overview with goal, dependencies, plan estimate
- [ ] Complete module structure (~30 source files, ~15 test files)
- [ ] 5 waves with file targets, dependencies, and success criteria
- [ ] API contracts for all major classes/functions with Python type hints
- [ ] Test strategy with test categories, scenarios, infrastructure, and count estimates
- [ ] All 5 LIVE requirements mapped to implementation-level acceptance criteria
- [ ] Phase 15 success criteria (measurable)
- [ ] Risks and mitigations
- [ ] Plan mapping (estimated plan count and sizing for Phase 15)
- [ ] Cross-references to 14-01-DESIGN.md and 14-02-DESIGN.md specifications
</verification>

<success_criteria>
14-03-BLUEPRINT.md exists with a complete Phase 15 implementation plan. The blueprint contains 5 waves, ~30 file targets, ~15 API contracts with type hints, ~125 estimated tests, and maps to 8-9 GSD plans. A `/gsd:plan-phase` invocation for Phase 15 could use this blueprint as its primary input without additional research.
</success_criteria>

<output>
After completion, create `.planning/phases/14-live-session-governance-research/14-03-SUMMARY.md`
</output>
