---
phase: 15-ddf-detection-substrate
plan: 07
type: execute
wave: 5
depends_on: ["15-06"]
files_modified:
  - tests/test_ddf_integration.py
autonomous: true

must_haves:
  truths:
    - "End-to-end pipeline run produces flame_events from real-like session data"
    - "Level 6 deposit path verified: Tier 1 -> Tier 2 enrichment -> deposit to memory_candidates"
    - "O_AXS detection produces correct episode segmentation"
    - "IntelligenceProfile CLI displays profile from pipeline-produced data"
    - "All 10 DDF requirements (DDF-01 through DDF-10) have at least one integration test"
  artifacts:
    - path: "tests/test_ddf_integration.py"
      provides: "Integration tests covering full DDF pipeline and all 10 requirements"
      min_lines: 150
  key_links:
    - from: "tests/test_ddf_integration.py"
      to: "src/pipeline/runner.py"
      via: "runs PipelineRunner.run_session with DDF-enabled config"
      pattern: "run_session"
---

<objective>
Validate the full DDF pipeline with integration tests covering all 10 DDF requirements. These tests run the complete pipeline with synthetic session data and verify end-to-end behavior.

Purpose: Integration tests ensure that the schema, detectors, enrichment, deposit, metrics, and CLI all work together. Each DDF requirement is verified by at least one test. This is the final validation before Phase 15 is marked complete.
Output: Integration test file with ~15 tests covering all 10 DDF requirements.
</objective>

<execution_context>
@/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ddf-detection-substrate/15-CONTEXT.md
@.planning/phases/15-ddf-detection-substrate/15-01-SUMMARY.md
@.planning/phases/15-ddf-detection-substrate/15-02-SUMMARY.md
@.planning/phases/15-ddf-detection-substrate/15-03-SUMMARY.md
@.planning/phases/15-ddf-detection-substrate/15-04-SUMMARY.md
@.planning/phases/15-ddf-detection-substrate/15-05-SUMMARY.md
@.planning/phases/15-ddf-detection-substrate/15-06-SUMMARY.md
@src/pipeline/runner.py
@src/pipeline/ddf/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests for all 10 DDF requirements</name>
  <files>
    tests/test_ddf_integration.py
  </files>
  <action>
Create a comprehensive integration test file that validates each DDF requirement end-to-end.

**Test fixture setup:**
- Create a shared pytest fixture `ddf_pipeline` that:
  - Creates in-memory DuckDB
  - Calls create_schema() (which now includes DDF schema)
  - Calls create_review_schema() (for memory_candidates)
  - Loads default config from data/config.yaml
  - Returns (conn, config) tuple

- Create fixture `seeded_flame_events` that:
  - Seeds flame_events with a mix of L0-7 markers, both human and AI subjects
  - Seeds some with flood_confirmed=True, axis_identified set
  - Seeds across 3 sessions with 2 human_ids

- Create fixture `synthetic_session_events` that:
  - Builds a list of event dicts mimicking a real JSONL session
  - Includes human messages with trunk identification language (L0)
  - Includes human messages with causal language (L1, L2)
  - Includes a granularity drop + novel concept pair (O_AXS)
  - Includes AI messages with CCD patterns

**Integration tests (~15 tests covering DDF-01 through DDF-10):**

DDF-01: O_AXS valid episode mode
1. `test_ddf01_o_axs_is_valid_mode` -- Create a TaggedEvent with label='O_AXS', verify segmenter opens an episode
2. `test_ddf01_o_axs_detector_in_pipeline` -- Run detect on synthetic events, verify O_AXS events produced

DDF-02: flame_events human markers
3. `test_ddf02_flame_events_human_markers` -- Write L0-7 human markers, query flame_events WHERE subject='human', verify all present
4. `test_ddf02_detection_source_field` -- Verify detection_source='stub' for Tier 1, 'opeml' for Tier 2

DDF-03: ai_flame_events + write-on-detect
5. `test_ddf03_ai_flame_events_view` -- Write AI markers, query ai_flame_events view, verify only subject='ai' returned
6. `test_ddf03_write_on_detect_deposit` -- Seed L6 flame_event with flood_confirmed=True and axis_identified, call deposit_level6, verify memory_candidates row exists with source_flame_event_id

DDF-04: IntelligenceProfile
7. `test_ddf04_intelligence_profile_aggregation` -- Seed flame_events, compute profile, verify all 6 metrics correct
8. `test_ddf04_ai_profile_separate` -- Verify AI profile aggregates only subject='ai' events

DDF-05: GeneralizationRadius
9. `test_ddf05_generalization_radius` -- Seed session_constraint_eval with varying scope paths, compute radius, verify correct counts
10. `test_ddf05_stagnation_detection` -- Seed constraint with radius=1, firing_count=10, verify is_stagnant=True

DDF-06: Spiral tracking
11. `test_ddf06_spiral_ascending_scopes` -- Seed constraint evaluated in sessions with ascending scope diversity, verify spiral detected

DDF-07: Epistemological origin
12. `test_ddf07_epistemological_on_constraint` -- Extract constraint from block episode, verify epistemological_origin='reactive'
13. `test_ddf07_default_is_principled` -- Extract constraint with no strong signal, verify epistemological_origin='principled'

DDF-08: Intelligence CLI
14. `test_ddf08_cli_profile_output` -- Seed data, invoke CLI via CliRunner, verify output contains all metrics

DDF-09: False Integration
15. `test_ddf09_false_integration_marker` -- Create episode with 2+ distinct scope prefixes, run FalseIntegrationDetector, verify ai_flame_events marker or axis_hypotheses row

DDF-10: Causal Isolation Query
16. `test_ddf10_causal_isolation_records` -- Seed premise_registry with foil_path_outcomes, run CausalIsolationRecorder, verify flame_events with marker_type in ('causal_isolation_success', 'causal_isolation_failed', 'missing_isolation')

**Final regression check:**
17. `test_existing_tests_still_pass` -- Meta-test that just verifies the total test count has increased, not decreased (count current test files)

Each test is self-contained with its own fixture data. Tests use in-memory DuckDB (no file I/O).
  </action>
  <verify>python -m pytest tests/test_ddf_integration.py -v && python -m pytest tests/ -x --timeout=180 -q 2>&1 | tail -10</verify>
  <done>All 10 DDF requirements verified by integration tests. End-to-end deposit path confirmed. Zero regressions in existing test suite.</done>
</task>

</tasks>

<verification>
1. All 17 integration tests pass
2. Each DDF requirement (01-10) has at least one dedicated integration test
3. The deposit path is verified end-to-end: Tier 1 -> Tier 2 -> Level 6 -> memory_candidates
4. O_AXS episode segmentation verified
5. Full test suite passes: `python -m pytest tests/ -x --timeout=180`
</verification>

<success_criteria>
- 15+ integration tests pass covering all DDF-01 through DDF-10
- End-to-end deposit path verified (flame_events -> enrichment -> memory_candidates)
- O_AXS produces valid episode segments
- Intelligence CLI produces correct output from seeded data
- Zero regressions in existing 1227+ tests
- Phase 15 success criteria from ROADMAP.md all verifiable
</success_criteria>

<output>
After completion, create `.planning/phases/15-ddf-detection-substrate/15-07-SUMMARY.md`
</output>
