---
phase: 25-genus-protocol-propagation
plan: 04
type: execute
wave: 3
depends_on: ["25-02", "25-03"]
files_modified:
  - ~/.claude/skills/genus-first/SKILL.md
  - ~/.claude/skills/genus-first/genus-framework.md
  - tests/pipeline/live/test_genus_consult_integration.py
autonomous: true

must_haves:
  truths:
    - "/genus-first SKILL.md Step 3 detects three capability tiers: OPE mode, Bus mode, Lightweight mode"
    - "Bus mode activates when data/ope.db absent AND OPE_BUS_SOCKET is set"
    - "In Bus mode, /genus-first POSTs to /api/genus-consult and embeds the response in output"
    - "genus-framework.md 'Future Capability' section replaced with actual bus mode documentation"
    - "Integration test: simulated non-OPE session receives valid genus response for A7/CRAD via bus"
  artifacts:
    - path: "~/.claude/skills/genus-first/SKILL.md"
      provides: "Three-tier capability detection with Bus mode path"
      contains: "OPE_BUS_SOCKET"
    - path: "~/.claude/skills/genus-first/genus-framework.md"
      provides: "Updated Mode A with Bus sub-mode documentation"
      contains: "Bus-Mediated"
    - path: "tests/pipeline/live/test_genus_consult_integration.py"
      provides: "Integration test for genus-consult round-trip"
      contains: "test_genus_consult"
  key_links:
    - from: "~/.claude/skills/genus-first/SKILL.md"
      to: "/api/genus-consult"
      via: "POST via _post_json or curl to OPE_BUS_SOCKET"
      pattern: "genus-consult"
    - from: "tests/pipeline/live/test_genus_consult_integration.py"
      to: "src/pipeline/live/bus/server.py"
      via: "httpx AsyncClient against create_app with seeded data"
      pattern: "genus-consult"
---

<objective>
Extend /genus-first SKILL.md with a Bus mode path (Mode A sub-mode) and update genus-framework.md to document the now-implemented bus capability. Add an integration test proving a non-OPE session can query genus-consult via the bus and receive a valid A7/CRAD genus response.

Purpose: This plan completes the genus-first propagation loop. After this, any Claude Code session with OPE_BUS_SOCKET set can invoke /genus-first and get OPE-powered genus identification without importing OPE directly. The integration test validates Success Criterion 5 from the roadmap.
Output: Updated SKILL.md and genus-framework.md, passing integration test.
</objective>

<execution_context>
@/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/david/projects/orchestrator-policy-extraction/.planning/PROJECT.md
@/Users/david/projects/orchestrator-policy-extraction/.planning/ROADMAP.md
@/Users/david/projects/orchestrator-policy-extraction/.planning/STATE.md
@/Users/david/projects/orchestrator-policy-extraction/.planning/phases/25-genus-protocol-propagation/25-RESEARCH.md
@/Users/david/.claude/skills/genus-first/SKILL.md
@/Users/david/.claude/skills/genus-first/genus-framework.md
@/Users/david/projects/orchestrator-policy-extraction/src/pipeline/live/bus/server.py
@/Users/david/projects/orchestrator-policy-extraction/tests/test_bus_foundation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend /genus-first SKILL.md with Bus mode and update genus-framework.md</name>
  <files>
    ~/.claude/skills/genus-first/SKILL.md
    ~/.claude/skills/genus-first/genus-framework.md
  </files>
  <action>
**In `~/.claude/skills/genus-first/SKILL.md`:**

Replace the Step 3 capability detection section with a three-tier detection:

```markdown
## Step 3: Detect Capabilities

Check for available machinery -- run this before reading any other context:

```bash
# Check for OPE mode (3 tiers)
if test -f src/pipeline/premise/fundamentality.py || test -f data/ope.db; then
    echo "OPE_LOCAL_MODE"
elif [ -z "${OPE_BUS_SOCKET:-}" ]; then
    echo "LIGHTWEIGHT_MODE"
else
    echo "OPE_BUS_MODE"
fi
```

Set your operating mode:
- **OPE-local mode:** `src/pipeline/premise/fundamentality.py` exists OR `data/ope.db` exists -- full OPE machinery available locally
- **OPE-via-bus mode:** `data/ope.db` absent AND `OPE_BUS_SOCKET` environment variable is set -- genus oracle available remotely via governance bus
- **Lightweight mode:** neither present -- MEMORY.md + git history only

Announce the mode at the top of your output: `Mode: OPE-local` or `Mode: OPE-via-bus` or `Mode: Lightweight`
```

In Step 4 ("Read Project Context"), after the existing OPE mode section that queries axis_edges directly, add a Bus mode section:

```markdown
**If OPE-via-bus mode:** query the governance bus for prior genera:
```bash
# POST to bus genus oracle for matching genera
curl -s --unix-socket "$OPE_BUS_SOCKET" \
  -X POST http://localhost/api/genus-consult \
  -H "Content-Type: application/json" \
  -d "{\"problem\": \"[INSERT PROBLEM HERE]\", \"session_id\": \"$(echo $OPE_SESSION_ID)\", \"repo\": \"$(basename $(pwd))\"}" \
  2>/dev/null || echo "{}"
```

If the bus returns a non-null genus with confidence >= 0.3, use it as a pre-validated genus. Include the returned instances as citable instances in the genus-first prompt.
```

In Step 6 ("Generate the Genus-First Prompt"), after the OPE mode staging note, add:

```markdown
**OPE-via-bus mode only:** after the prompt, add:
```
[OPE bus note: the genus oracle returned genus "[genus name]" with
confidence [N]. This genus has [M] prior instances from OPE's history.
If you use it, include it in a PREMISE block with the GENUS: field.]
```
```

In the "Operating Principles" section at the end, update the modes list:

```markdown
- **Mode A has two sub-modes:** OPE-local (full pipeline access) and OPE-via-bus (genus oracle only). Both produce equivalent genus-first prompts. OPE-local can additionally stage genus to JSONL; OPE-via-bus is read-only.
- **Mode B is complete:** Lightweight mode produces a fully valid genus-first prompt using only MEMORY.md + git log. It is not a degraded experience.
```

**In `~/.claude/skills/genus-first/genus-framework.md`:**

Replace the "## Future Capability: Bus-Mediated Genus Oracle" section (lines 106-108) with:

```markdown
## Bus-Mediated Genus Oracle (Phase 25)

A co-governed session (modernizing tool, objectivism library, etc.) can POST to the OPE governance bus (`/api/genus-consult`) with a problem description:

```json
POST /api/genus-consult
{
  "problem": "per-file searchability is broken for minority files",
  "session_id": "session-123",
  "repo": "objectivism-library"
}
```

OPE responds with the best-matching genus from its `axis_edges` history:

```json
{
  "genus": "corpus-relative identity retrieval",
  "instances": ["A7 per-file searchability failure", "MOTM dedup failure"],
  "valid": true,
  "confidence": 0.75
}
```

The response includes:
- **genus**: Best-matching genus name from axis_edges (null if no match)
- **instances**: Up to 2 citable instances from the genus evidence (empty if no match)
- **valid**: true when instances >= 2 (fundamentality criterion met)
- **confidence**: Token-overlap score 0.0-1.0 (matched tokens / genus name tokens)

This capability is activated in `/genus-first` Step 3 when `data/ope.db` is absent and `OPE_BUS_SOCKET` is set (OPE-via-bus mode).
```
  </action>
  <verify>
Read both files and verify:
1. SKILL.md Step 3 has three-tier detection (OPE_LOCAL_MODE, OPE_BUS_MODE, LIGHTWEIGHT_MODE)
2. SKILL.md Step 4 has bus mode section with curl command
3. SKILL.md Step 6 has bus mode note
4. genus-framework.md no longer says "Future Capability" -- replaced with "Phase 25" implementation docs
  </verify>
  <done>
/genus-first SKILL.md has three-tier capability detection with Bus mode path. genus-framework.md documents the implemented bus-mediated genus oracle. Bus mode activates when data/ope.db absent and OPE_BUS_SOCKET set.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration test -- non-OPE session queries genus-consult and receives A7/CRAD response</name>
  <files>
    tests/pipeline/live/test_genus_consult_integration.py
  </files>
  <action>
Create `tests/pipeline/live/test_genus_consult_integration.py`. This test validates Success Criterion 5: a simulated non-OPE session receives a valid genus response for the A7/CRAD entry from axis_edges.

```python
"""Integration test for /api/genus-consult endpoint.

Simulates a non-OPE session (data/ope.db absent, OPE_BUS_SOCKET set)
querying the bus for genus identification. Seeds axis_edges with the
A7/CRAD genus_of entry and verifies the full round-trip.
"""
import json
import pytest
import duckdb
from httpx import AsyncClient, ASGITransport

from src.pipeline.live.bus.server import create_app
from src.pipeline.live.bus.schema import create_bus_schema
from src.pipeline.ddf.topology.schema import create_topology_schema


@pytest.fixture
def db_path(tmp_path):
    return str(tmp_path / "test_integration.db")


@pytest.fixture
def seeded_app(db_path):
    """Create app with seeded genus_of data in axis_edges."""
    # Pre-seed the database with both schemas + genus data
    conn = duckdb.connect(db_path)
    create_topology_schema(conn)  # creates axis_edges table
    create_bus_schema(conn)   # creates bus_sessions

    # Seed the A7/CRAD genus_of edge
    conn.execute(
        "INSERT INTO axis_edges (edge_id, axis_a, axis_b, relationship_text, "
        "activation_condition, evidence, abstraction_level, status, "
        "trunk_quality, created_session_id, created_at) VALUES "
        "(?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
        [
            "edge-crad-integration",
            "corpus-relative identity retrieval",
            "searchability-failure",
            "genus_of",
            "problem-context",
            json.dumps({
                "instances": [
                    "A7 per-file searchability failure",
                    "MOTM dedup failure",
                ],
                "source": "genus_check_gate",
                "session_id": "ope-session-1",
            }),
            3,
            "candidate",
            0.8,
            "ope-session-1",
            "2026-02-28T00:00:00Z",
        ],
    )

    # Register a bus_session for repo scoping
    conn.execute(
        "INSERT INTO bus_sessions (session_id, run_id, registered_at, repo) "
        "VALUES (?, ?, ?, ?)",
        ["ope-session-1", "run-1", "2026-02-28T00:00:00Z",
         "orchestrator-policy-extraction"],
    )
    conn.close()

    return create_app(db_path=db_path)


@pytest.fixture
def client(seeded_app):
    transport = ASGITransport(app=seeded_app)
    return AsyncClient(transport=transport, base_url="http://test")


@pytest.mark.asyncio
async def test_genus_consult_returns_crad_for_identity_retrieval(client):
    """Non-OPE session receives valid genus for identity retrieval problem."""
    resp = await client.post("/api/genus-consult", json={
        "problem": "corpus relative identity retrieval issue with searchability",
        "session_id": "non-ope-session-1",
        "repo": "orchestrator-policy-extraction",
    })
    assert resp.status_code == 200
    data = resp.json()
    assert data["genus"] == "corpus-relative identity retrieval"
    assert data["valid"] is True
    assert data["confidence"] > 0.5
    assert len(data["instances"]) == 2
    assert "A7 per-file searchability failure" in data["instances"]


@pytest.mark.asyncio
async def test_genus_consult_returns_null_for_unrelated_problem(client):
    """Unrelated problem returns null genus."""
    resp = await client.post("/api/genus-consult", json={
        "problem": "database migration SQL syntax error",
        "session_id": "non-ope-session-2",
    })
    assert resp.status_code == 200
    data = resp.json()
    assert data["genus"] is None
    assert data["valid"] is False
    assert data["confidence"] == 0.0
    assert data["instances"] == []


@pytest.mark.asyncio
async def test_genus_consult_fails_open_on_empty_body(client):
    """Empty body returns null genus, not error."""
    resp = await client.post("/api/genus-consult", json={})
    assert resp.status_code == 200
    data = resp.json()
    assert data["genus"] is None
    assert data["confidence"] == 0.0


@pytest.mark.asyncio
async def test_genus_consult_repo_scoping(client):
    """Query with wrong repo returns no match (session not in that repo)."""
    resp = await client.post("/api/genus-consult", json={
        "problem": "corpus relative identity retrieval",
        "session_id": "non-ope-session-3",
        "repo": "some-other-repo",
    })
    assert resp.status_code == 200
    data = resp.json()
    # No genus_of edges from sessions in "some-other-repo"
    assert data["genus"] is None


@pytest.mark.asyncio
async def test_genus_consult_instance_name_boost(client):
    """Problem mentioning A7 should match via instance name boost."""
    resp = await client.post("/api/genus-consult", json={
        "problem": "A7 per-file searchability failure keeps recurring",
        "session_id": "non-ope-session-4",
    })
    assert resp.status_code == 200
    data = resp.json()
    # Should match via instance boost even if genus name tokens
    # don't fully match
    assert data["genus"] == "corpus-relative identity retrieval"
    assert data["confidence"] > 0.0
```

Test structure:
- Uses `seeded_app` fixture with pre-seeded axis_edges + bus_sessions
- Uses httpx AsyncClient with ASGITransport (same pattern as test_push_links.py)
- 5 test cases covering: successful match, no match, fail-open, repo scoping, instance boost
- Each test simulates a "non-OPE session" (different session_id, querying via bus endpoint)
  </action>
  <verify>
Run: `python -m pytest tests/pipeline/live/test_genus_consult_integration.py -v`

All 5 tests pass.
  </verify>
  <done>
Integration test validates that a non-OPE session can query /api/genus-consult and receive the A7/CRAD genus response with correct instances, confidence, and valid flag. Repo scoping and fail-open behavior verified. Success Criterion 5 from the roadmap is proven.
  </done>
</task>

</tasks>

<verification>
Full plan verification:
1. `python -m pytest tests/pipeline/live/test_genus_consult_integration.py -v` -- all 5 tests pass
2. `grep -c 'OPE_BUS_SOCKET' ~/.claude/skills/genus-first/SKILL.md` -- >= 2 occurrences
3. `grep -c 'Future Capability' ~/.claude/skills/genus-first/genus-framework.md` -- 0 (replaced)
4. `grep -c 'Phase 25' ~/.claude/skills/genus-first/genus-framework.md` -- >= 1
5. `python -m pytest tests/test_bus_foundation.py -v` -- no regressions
</verification>

<success_criteria>
- /genus-first SKILL.md has three-tier detection (OPE-local, OPE-via-bus, Lightweight)
- Bus mode POSTs to /api/genus-consult and embeds response
- genus-framework.md documents implemented bus oracle (not "future")
- Integration test proves non-OPE session receives A7/CRAD genus via bus
- All existing bus tests pass
</success_criteria>

<output>
After completion, create `/Users/david/projects/orchestrator-policy-extraction/.planning/phases/25-genus-protocol-propagation/25-04-SUMMARY.md`
</output>
