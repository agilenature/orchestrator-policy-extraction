---
phase: 14.1-premise-registry-premise-assertion-gate
verified: 2026-02-23T23:52:06Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---

# Phase 14.1: Premise Registry and Premise-Assertion Gate Verification Report

**Phase Goal:** Build the introspective layer that validates premise correctness at write-class tool call boundaries. The CLAUDE.md declaration protocol makes AI premises explicit; the Premise Registry stores and tracks them; the PAG hook validates them before mutation. Three temporal modes: retrospective (existing OPE pipeline), introspective (real-time premise validation at write boundary), projective (foil instantiation predicts divergence from historical premises).

**Verified:** 2026-02-23T23:52:06Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                         | Status     | Evidence                                                                                                     |
|-----|---------------------------------------------------------------------------------------------------------------|------------|--------------------------------------------------------------------------------------------------------------|
| 1   | premise_registry DuckDB table exists with all 20 columns matching the spec                                     | VERIFIED   | create_schema() + live DuckDB query confirms 20 columns with correct types (TIMESTAMPTZ, INTEGER, JSON, VARCHAR) |
| 2   | PreToolUse hook reads PREMISE blocks at write-class boundaries; emits warnings; never blocks                  | VERIFIED   | premise_gate.py: main() with WRITE_CLASS gate, parse_premise_blocks() call, append_to_staging(), always exits 0 |
| 3   | Foil instantiation retrieves historical episodes where foil premise was active; emits PROJECTION_WARNING       | VERIFIED   | _check_foil_instantiation() in premise_gate.py; FoilInstantiator.instantiate() in foil.py with three-tier matching |
| 4   | Staining pipeline marks premises stained when AmnesiaDetector retrospectively invalidates; propagates via derivation_chain | VERIFIED | StainingPipeline.stain_from_amnesia(), propagate_staining() with visited set + max_depth=20; runner.py Step 14.5 |
| 5   | episodes table extended with parent_episode_id (nullable VARCHAR); runner.py sets it during population        | VERIFIED   | write_episodes() MERGE includes parent_episode_id (7 occurrences); runner.py lines 302, 323, 349 set prev_episode_id |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact                                          | Status     | Level 1: Exists | Level 2: Substantive | Level 3: Wired            |
|---------------------------------------------------|------------|-----------------|----------------------|---------------------------|
| `src/pipeline/premise/schema.py`                 | VERIFIED   | YES (121 lines) | YES — DDL, indexes, backfill SQL, create_premise_schema() | YES — imported by storage/schema.py create_schema() |
| `src/pipeline/premise/models.py`                 | VERIFIED   | YES (126 lines) | YES — ParsedPremise + PremiseRecord with all 20 fields, make_id() classmethod | YES — imported by parser.py, registry.py, ingestion.py, premise_gate.py |
| `src/pipeline/premise/registry.py`               | VERIFIED   | YES (354 lines) | YES — full CRUD: register, get, get_by_session, get_stained, update_staleness, stain, find_by_foil, update_derivation_depth, count, backfill_parent_episodes | YES — imported by foil.py, staining.py, ingestion.py, premise_gate.py |
| `src/pipeline/premise/parser.py`                 | VERIFIED   | YES (186 lines) | YES — PREMISE_BLOCK_RE, FOIL_VERIFIED_BLOCK_RE, parse_premise_blocks(), cross-premise reference detection via _PREMISE_ID_REF_RE | YES — imported by premise_gate.py |
| `src/pipeline/premise/foil.py`                   | VERIFIED   | YES (381 lines) | YES — FoilInstantiator with three-tier matching (exact + keyword), detect_divergence(), FoilMatch + DivergenceNode models | YES — imported by premise/__init__.py; _check_foil_instantiation uses PremiseRegistry.find_by_foil() |
| `src/pipeline/premise/staining.py`               | VERIFIED   | YES (218 lines) | YES — StainingPipeline.stain_from_amnesia(), propagate_staining() BFS with visited set, stain_from_policy_violation() | YES — imported by ingestion.py; AmnesiaEvent from durability/amnesia.py wired |
| `src/pipeline/premise/ingestion.py`              | VERIFIED   | YES (216 lines) | YES — ingest_staging() with Begging the Question detection + derivation_depth computation; run_staining() | YES — imported by runner.py Steps 11.5 and 14.5 |
| `src/pipeline/premise/transcript.py`            | VERIFIED   | YES (163 lines) | YES — read_recent_assistant_text() backward scan, count_validation_calls_since_last_user() | YES — imported by premise_gate.py |
| `src/pipeline/premise/staging.py`               | VERIFIED   | YES (substantive) | YES — append_to_staging() with fcntl.flock, read_staging(), clear_staging(), STAGING_PATH constant | YES — imported by premise_gate.py (write) and ingestion.py (read/clear) |
| `src/pipeline/live/hooks/premise_gate.py`        | VERIFIED   | YES (356 lines) | YES — main(), _is_high_risk_path(), _check_stained_premises(), _check_foil_instantiation(), _check_ad_ignorantiam() | YES — imports parse_premise_blocks, read_recent_assistant_text, append_to_staging, find_by_foil |
| `src/pipeline/storage/schema.py` (updated)       | VERIFIED   | YES | YES — create_premise_schema() called at line 403–405; "premise_registry" in drop_schema() at line 417 | YES — wired: lazy import inside create_schema() |
| `src/pipeline/storage/writer.py` (updated)       | VERIFIED   | YES | YES — parent_episode_id in MERGE staging, SELECT, UPDATE SET, INSERT (7 occurrences confirmed) | YES — wired: used in write_episodes() |
| `src/pipeline/runner.py` (updated)               | VERIFIED   | YES | YES — Step 11.5 (ingest_staging, lines 422–442) and Step 14.5 (run_staining + amnesia re-read, lines 634–684) with lazy imports | YES — wired: both steps executed during batch pipeline run |

---

### Key Link Verification

| From                                          | To                                          | Via                                                       | Status     | Details                                                                           |
|-----------------------------------------------|---------------------------------------------|-----------------------------------------------------------|------------|-----------------------------------------------------------------------------------|
| `src/pipeline/premise/schema.py`              | `src/pipeline/storage/schema.py`            | create_schema() calls create_premise_schema() (line 403) | WIRED      | Lazy import inside create_schema() to avoid circular import                       |
| `src/pipeline/runner.py`                      | `episodes` table                            | parent_episode_id set via prev_episode_id tracking (lines 302, 323, 349) | WIRED | First episode gets None, subsequent get prev episode_id |
| `src/pipeline/premise/registry.py`            | `src/pipeline/premise/models.py`            | PremiseRecord imported and used in register(), get(), _row_to_record() | WIRED | All CRUD methods use PremiseRecord model |
| `src/pipeline/storage/writer.py`              | `episodes` table                            | parent_episode_id in MERGE column list (confirmed via grep: 7 occurrences) | WIRED | Causal link not silently dropped |
| `src/pipeline/live/hooks/premise_gate.py`     | `src/pipeline/premise/transcript.py`        | import read_recent_assistant_text (line 35)               | WIRED      | Called in main() to extract recent assistant text |
| `src/pipeline/live/hooks/premise_gate.py`     | `src/pipeline/premise/parser.py`            | import parse_premise_blocks (line 32)                     | WIRED      | Called for each text block in main() |
| `src/pipeline/live/hooks/premise_gate.py`     | `src/pipeline/premise/staging.py`           | import append_to_staging (line 33)                        | WIRED      | Called after building staging records in main() |
| `src/pipeline/live/hooks/premise_gate.py`     | `src/pipeline/premise/registry.py`          | PremiseRegistry.find_by_foil inside _check_foil_instantiation() | WIRED | Lazy import inside helper function |
| `src/pipeline/premise/staining.py`            | `src/pipeline/durability/amnesia.py`        | AmnesiaEvent imported at line 21; used in stain_from_amnesia() | WIRED | Direct import, used as typed parameter |
| `src/pipeline/premise/foil.py`                | `src/pipeline/premise/registry.py`          | find_by_foil called in instantiate() (line 133)           | WIRED      | Tier 1 exact matching |
| `src/pipeline/premise/ingestion.py`           | `src/pipeline/premise/staging.py`           | read_staging + clear_staging imported (line 24)           | WIRED      | read_staging called in ingest_staging(); clear_staging called after ingestion |
| `src/pipeline/runner.py`                      | `src/pipeline/premise/ingestion.py`         | ingest_staging called at line 430 (Step 11.5)            | WIRED      | Lazy import with ImportError fallback |
| `src/pipeline/runner.py`                      | `src/pipeline/premise/ingestion.py`         | run_staining called at line 672 (Step 14.5)               | WIRED      | Lazy import; amnesia events re-read from DuckDB at staining time |

---

### Requirements Coverage (ROADMAP Success Criteria)

| Requirement                                                                                                                                                                                                        | Status     | Verification Evidence                                                                   |
|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------|-----------------------------------------------------------------------------------------|
| 1. premise_registry DuckDB table with 20-column schema including derivation_depth, validation_calls_before_claim, derivation_chain JSONB; derivation_depth=0 = Begging the Question; validation_calls_before_claim=0 = Ad Ignorantiam | SATISFIED  | Live DuckDB query confirms 20 columns; ingest_staging() detects circular derivation_chain (Begging the Question); _check_ad_ignorantiam() fires when RQR=0 |
| 2. PreToolUse hook reads PREMISE blocks at write-class boundaries; emits block signal on UNVALIDATED high-risk; emits PROJECTION_WARNING with foil_path_outcomes                                                  | SATISFIED  | premise_gate.py WRITE_CLASS gate; _is_high_risk_path(); _check_foil_instantiation() emits PROJECTION_WARNING: FOIL_INSTANTIATED |
| 3. Foil instantiation query: given FOIL field, retrieves historical episodes where foil was active; identifies first action-divergence node; returns as PROJECTION_WARNING                                         | SATISFIED  | FoilInstantiator.instantiate() with exact+keyword tiers; detect_divergence() returns DivergenceNode; hook emits PROJECTION_WARNING: FOIL_INSTANTIATED |
| 4. Staining pipeline: Layer 5 retrospective invalidation stains premises; stained premises trigger PROJECTION_WARNING on next write-class use                                                                     | SATISFIED  | StainingPipeline.stain_from_amnesia(); runner.py Step 14.5; _check_stained_premises() emits PROJECTION_WARNING at hook time |
| 5. episodes table extended with parent_episode_id (nullable VARCHAR)                                                                                                                                              | SATISFIED  | Live DuckDB query confirms parent_episode_id in episodes; runner.py lines 302/323/349; write_episodes() MERGE propagates it |

---

### Anti-Patterns Found

| File                                                   | Line    | Pattern          | Severity | Impact                                                              |
|--------------------------------------------------------|---------|------------------|----------|---------------------------------------------------------------------|
| `src/pipeline/live/hooks/premise_gate.py`             | 344–349 | hookSpecificOutput missing "hookEventName" field | INFO | Plan spec showed {"hookEventName": "PreToolUse", "additionalContext": "..."} but implementation uses {"additionalContext": "..."} directly. Tests verify the implementation's format and all 30 hook tests pass. The Claude Code hooks protocol does not require hookEventName in the response body — it is supplied to the hook via stdin. No functional gap. |
| `src/pipeline/live/hooks/premise_gate.py`             | —       | Requires PYTHONPATH at invocation | INFO | Hook script requires PYTHONPATH=/path/to/project when invoked as a subprocess. Verified: PYTHONPATH set correctly causes exit 0. Tests use project test runner which handles sys.path. Hook registration in .claude/settings.json would need PYTHONPATH env var. Not a code defect — standard Python packaging constraint. |

No STUB or MISSING anti-patterns. No TODO/FIXME/placeholder comments in any premise module file. All implementations are substantive.

---

### Human Verification Required

None — all goal criteria are verifiable programmatically through the test suite (134 premise tests + 30 live hook tests = 164 tests total, all passing) and direct DuckDB schema inspection.

---

### Test Suite Summary

```
tests/pipeline/premise/   — 134 passed
tests/pipeline/live/      — 30 passed
Total premise-module tests — 164 passed
```

All test categories verified:
- `test_models.py` — PremiseRecord.make_id() determinism, frozen immutability (12 tests)
- `test_parser.py` — PREMISE blocks, FOIL_VERIFIED blocks, cross-premise reference detection (22 tests)
- `test_schema.py` — 20-column DDL, idempotency, parent_episode_id backfill (10 tests)
- `test_registry.py` — Full CRUD roundtrip, staining, staleness, find_by_foil, backfill (20 tests)
- `test_transcript.py` — Backward JSONL scan, validation call counting (18 tests)
- `test_staging.py` — append/read/clear with file locking (12 tests)
- `test_foil.py` — Three-tier matching, divergence detection (13 tests)
- `test_staining.py` — Amnesia staining, propagation (Stolen Concept), policy violation (13 tests)
- `test_ingestion.py` — JSONL-to-DuckDB bridge, Begging the Question detection (14 tests)
- `test_premise_gate.py` — Gate check, staging, fail-open, high-risk, stained, foil, Ad Ignorantiam (30 tests)

---

## Gaps Summary

No gaps. All five observable truths verified. All required artifacts exist, are substantive, and are wired. All ROADMAP success criteria satisfied. Two INFO-level observations noted (hookEventName field deviation from plan spec text, PYTHONPATH deployment requirement) — neither blocks goal achievement.

---

_Verified: 2026-02-23T23:52:06Z_
_Verifier: Claude (gsd-verifier)_
