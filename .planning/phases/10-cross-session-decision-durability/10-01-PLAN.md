---
phase: 10-cross-session-decision-durability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/schemas/constraint.schema.json
  - src/pipeline/constraint_store.py
  - src/pipeline/constraint_extractor.py
  - src/pipeline/escalation/constraint_gen.py
  - src/pipeline/storage/schema.py
  - src/pipeline/models/config.py
  - data/config.yaml
  - src/pipeline/durability/__init__.py
  - src/pipeline/durability/migration.py
  - tests/test_durability_migration.py
autonomous: true

must_haves:
  truths:
    - "Constraint schema allows type, status_history, and supersedes fields without validation failures"
    - "Existing constraints in data/constraints.json are migrated with type=behavioral_constraint and a bootstrap status_history entry"
    - "ConstraintStore supports point-in-time status lookup via get_status_at_time()"
    - "DuckDB has session_constraint_eval and amnesia_events tables with composite PK"
    - "New constraints from extractor and escalation generator include type and status_history fields"
  artifacts:
    - path: "data/schemas/constraint.schema.json"
      provides: "Updated schema with type, status_history, supersedes fields"
      contains: "status_history"
    - path: "src/pipeline/constraint_store.py"
      provides: "ConstraintStore with temporal status methods"
      exports: ["ConstraintStore"]
    - path: "src/pipeline/durability/migration.py"
      provides: "Constraint migration script"
      exports: ["migrate_constraints"]
    - path: "src/pipeline/storage/schema.py"
      provides: "DuckDB session_constraint_eval and amnesia_events tables"
      contains: "session_constraint_eval"
    - path: "src/pipeline/models/config.py"
      provides: "DurabilityConfig sub-model"
      contains: "DurabilityConfig"
  key_links:
    - from: "data/schemas/constraint.schema.json"
      to: "src/pipeline/constraint_store.py"
      via: "Schema validation on add()"
      pattern: "additionalProperties.*false"
    - from: "src/pipeline/durability/migration.py"
      to: "data/constraints.json"
      via: "Migrates existing constraints"
      pattern: "status_history"
    - from: "src/pipeline/constraint_extractor.py"
      to: "data/schemas/constraint.schema.json"
      via: "New constraints include type field"
      pattern: "behavioral_constraint"
---

<objective>
Extend the constraint schema, migrate existing constraints, add DuckDB evaluation tables, and create the DurabilityConfig model -- the foundation for cross-session decision durability.

Purpose: All downstream Phase 10 code (evaluator, amnesia detector, CLI, reporter) depends on: (1) the constraint schema allowing `type`, `status_history`, and `supersedes` fields, (2) DuckDB having evaluation storage tables, (3) the ConstraintStore supporting temporal status queries. This plan establishes all three.

Output: Updated constraint.schema.json, migrated constraints.json, extended ConstraintStore, DuckDB schema with 2 new tables, DurabilityConfig model.
</objective>

<execution_context>
@/Users/david/.claude/get-shit-done/workflows/execute-plan.md
@/Users/david/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@data/schemas/constraint.schema.json
@src/pipeline/constraint_store.py
@src/pipeline/constraint_extractor.py
@src/pipeline/escalation/constraint_gen.py
@src/pipeline/storage/schema.py
@src/pipeline/models/config.py
@data/config.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Constraint schema migration + ConstraintStore temporal methods</name>
  <files>
    data/schemas/constraint.schema.json
    src/pipeline/constraint_store.py
    src/pipeline/constraint_extractor.py
    src/pipeline/escalation/constraint_gen.py
    src/pipeline/durability/__init__.py
    src/pipeline/durability/migration.py
    tests/test_durability_migration.py
  </files>
  <action>
CRITICAL: Update constraint.schema.json FIRST before any other changes. The schema uses `"additionalProperties": false` -- new fields will fail validation if added to constraints before the schema allows them.

1. **Update `data/schemas/constraint.schema.json`:**
   Add three new properties to the `properties` object:
   - `type`: `{"type": "string", "enum": ["behavioral_constraint", "architectural_decision"], "description": "Category of this constraint"}`
   - `status_history`: `{"type": "array", "items": {"type": "object", "required": ["status", "changed_at"], "additionalProperties": false, "properties": {"status": {"type": "string", "enum": ["active", "candidate", "retired"]}, "changed_at": {"type": "string", "format": "date-time"}}}, "description": "Chronological status change log for point-in-time lookups"}`
   - `supersedes`: `{"type": ["string", "null"], "description": "ID of a prior constraint this one supersedes (for architectural decisions)"}`

2. **Create `src/pipeline/durability/__init__.py`:** Empty init file for the durability module.

3. **Create `src/pipeline/durability/migration.py`:**
   - Function `migrate_constraints(path: Path = Path("data/constraints.json"), schema_path: Path = Path("data/schemas/constraint.schema.json")) -> int`
   - For each constraint missing `type`: set `type = "behavioral_constraint"`
   - For each constraint missing `status_history`: bootstrap with `[{"status": constraint["status"], "changed_at": constraint["created_at"]}]`. If `created_at` is missing, use the earliest example's episode timestamp as approximation. If no examples, use empty list and log warning.
   - For each constraint missing `supersedes`: set `supersedes = None`
   - Validate each migrated constraint against schema (load schema, validate)
   - Write back to path
   - Return count of migrated constraints
   - Also provide a `if __name__ == "__main__"` block for standalone execution

4. **Extend `src/pipeline/constraint_store.py`:**
   - Add method `get_status_at_time(constraint_id: str, session_time: str) -> str | None`:
     Finds the constraint by ID, looks up `status_history`, returns the `status` from the last entry where `changed_at <= session_time`. Uses `datetime.fromisoformat()` for comparison (NOT string comparison -- pitfall 2). Returns None if no entry before session_time (constraint didn't exist yet). Falls back to current `status` field if `status_history` is empty/missing.
   - Add method `add_status_history_entry(constraint_id: str, status: str, changed_at: str) -> bool`:
     Appends a new entry to the constraint's `status_history`. Returns True if found and updated, False if constraint_id not found.
   - Add method `get_by_type(constraint_type: str) -> list[dict]`:
     Returns constraints filtered by `type` field.
   - Add method `get_active_constraints() -> list[dict]`:
     Returns all constraints where `status == "active"` (convenience for evaluator).

5. **Update `src/pipeline/constraint_extractor.py`:**
   In the `extract()` method, when building the constraint dict, add:
   - `"type": "behavioral_constraint"` field
   - `"status_history": [{"status": "active", "changed_at": created_at_value}]` where `created_at_value` is the same ISO8601 string used for the `created_at` field

6. **Update `src/pipeline/escalation/constraint_gen.py`:**
   In the `generate()` method, when building the constraint dict, add:
   - `"type": "behavioral_constraint"` field
   - `"status_history": [{"status": "candidate", "changed_at": created_at_value}]` (escalation-inferred constraints start as `candidate`)

7. **Create `tests/test_durability_migration.py`:**
   - Test migration adds `type`, `status_history`, `supersedes` to constraints missing them
   - Test migration preserves existing `type`/`status_history` if already present (idempotent)
   - Test migration bootstraps `status_history` from `created_at`
   - Test migration handles missing `created_at` gracefully
   - Test `get_status_at_time()` returns correct status at various points
   - Test `get_status_at_time()` returns None for timestamps before first history entry
   - Test `get_status_at_time()` uses datetime comparison (not string) for timezone safety
   - Test `add_status_history_entry()` appends correctly
   - Test `get_by_type()` filters correctly
   - Test `get_active_constraints()` returns only active
   - Use `tmp_path` for test isolation (no touching real constraints.json)
  </action>
  <verify>
    Run: `python -m pytest tests/test_durability_migration.py -v`
    All migration and ConstraintStore temporal method tests pass.
    Run: `python -c "from src.pipeline.durability.migration import migrate_constraints; print('import ok')"`
    Run: `python -c "from src.pipeline.constraint_store import ConstraintStore; print('import ok')"`
  </verify>
  <done>
    constraint.schema.json allows type, status_history, supersedes. Migration script converts existing constraints. ConstraintStore has get_status_at_time(), add_status_history_entry(), get_by_type(), get_active_constraints(). Constraint extractor and escalation generator produce constraints with type and status_history. 10+ tests passing.
  </done>
</task>

<task type="auto">
  <name>Task 2: DuckDB evaluation tables + DurabilityConfig + shared scope utility</name>
  <files>
    src/pipeline/storage/schema.py
    src/pipeline/models/config.py
    data/config.yaml
    src/pipeline/utils.py
  </files>
  <action>
1. **Update `src/pipeline/storage/schema.py`:**
   Add two new table creation blocks at the end of `create_schema()`, AFTER the escalation columns block:

   ```python
   # Phase 10: Decision durability evaluation tables
   conn.execute("""
       CREATE TABLE IF NOT EXISTS session_constraint_eval (
           session_id VARCHAR NOT NULL,
           constraint_id VARCHAR NOT NULL,
           eval_state VARCHAR NOT NULL,
           evidence_json JSON,
           scope_matched BOOLEAN NOT NULL DEFAULT TRUE,
           eval_ts TIMESTAMPTZ DEFAULT current_timestamp,
           PRIMARY KEY (session_id, constraint_id)
       )
   """)

   conn.execute("""
       CREATE TABLE IF NOT EXISTS amnesia_events (
           amnesia_id VARCHAR PRIMARY KEY,
           session_id VARCHAR NOT NULL,
           constraint_id VARCHAR NOT NULL,
           constraint_type VARCHAR,
           severity VARCHAR,
           evidence_json JSON,
           detected_at TIMESTAMPTZ DEFAULT current_timestamp
       )
   """)
   ```

   Add indexes:
   - `idx_eval_constraint ON session_constraint_eval(constraint_id)`
   - `idx_eval_session ON session_constraint_eval(session_id)`
   - `idx_amnesia_session ON amnesia_events(session_id)`
   - `idx_amnesia_constraint ON amnesia_events(constraint_id)`

   Update `drop_schema()` to drop these tables (before `shadow_mode_results`).

2. **Add `DurabilityConfig` to `src/pipeline/models/config.py`:**
   ```python
   class DurabilityConfig(BaseModel):
       """Decision durability tracking settings (Phase 10)."""
       min_sessions_for_score: int = 3
       evidence_excerpt_max_chars: int = 500
   ```
   Add to `PipelineConfig`:
   ```python
   durability: DurabilityConfig = Field(default_factory=DurabilityConfig)
   ```

3. **Update `data/config.yaml`:**
   Add a `durability:` section with the defaults:
   ```yaml
   durability:
     min_sessions_for_score: 3
     evidence_excerpt_max_chars: 500
   ```

4. **Create `src/pipeline/utils.py`:**
   Extract the `scopes_overlap()` function into a shared utility to avoid circular imports between validation/layers.py and durability/evaluator.py (Pitfall 6).
   ```python
   def scopes_overlap(paths_a: list[str], paths_b: list[str]) -> bool:
       """Check if two sets of scope paths overlap using bidirectional prefix matching.

       An empty paths list means repo-wide scope (matches everything).
       """
       if not paths_a or not paths_b:
           return True
       for a in paths_a:
           for b in paths_b:
               if a.startswith(b) or b.startswith(a):
                   return True
       return False
   ```
   Note: Keep the original `_scopes_overlap()` in `validation/layers.py` intact (it's used there with episode_paths semantics where only constraint_paths empty = True). The new shared utility treats EITHER empty list as repo-wide, matching the locked decision semantics for session scope (empty session scope = repo-wide).
  </action>
  <verify>
    Run: `python -m pytest tests/ -v --tb=short -q` to confirm no existing tests break.
    Run: `python -c "from src.pipeline.storage.schema import create_schema, get_connection; conn = get_connection(':memory:'); create_schema(conn); print(conn.execute('SELECT table_name FROM information_schema.tables').fetchall())"` to confirm both new tables exist.
    Run: `python -c "from src.pipeline.utils import scopes_overlap; assert scopes_overlap([], ['src/']); assert scopes_overlap(['src/foo.py'], ['src/']); assert not scopes_overlap(['tests/'], ['src/']); print('ok')"`
    Run: `python -c "from src.pipeline.models.config import PipelineConfig; c = PipelineConfig(); print(c.durability.min_sessions_for_score)"`
  </verify>
  <done>
    DuckDB creates session_constraint_eval (composite PK) and amnesia_events tables idempotently. DurabilityConfig model with min_sessions_for_score=3 is wired into PipelineConfig. Shared scopes_overlap() utility in utils.py. All existing 542+ tests still pass.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -v --tb=short` -- all 542+ existing tests pass, new migration tests pass
- `python -c "import json; d = json.load(open('data/schemas/constraint.schema.json')); assert 'status_history' in d['properties']; assert 'type' in d['properties']; print('schema ok')"` -- schema has new fields
- `python -c "from src.pipeline.storage.schema import create_schema, get_connection; c = get_connection(':memory:'); create_schema(c); r = c.execute(\"SELECT column_name FROM information_schema.columns WHERE table_name = 'session_constraint_eval'\").fetchall(); print([x[0] for x in r])"` -- table has expected columns
</verification>

<success_criteria>
1. constraint.schema.json accepts type, status_history, supersedes fields
2. Migration script converts all existing constraints to include these fields
3. ConstraintStore.get_status_at_time() returns correct status using datetime comparison
4. DuckDB has session_constraint_eval table with (session_id, constraint_id) composite PK
5. DuckDB has amnesia_events table with amnesia_id PK
6. DurabilityConfig model is part of PipelineConfig
7. scopes_overlap() shared utility works with bidirectional prefix matching
8. All existing tests still pass (zero regression)
</success_criteria>

<output>
After completion, create `.planning/phases/10-cross-session-decision-durability/10-01-SUMMARY.md`
</output>
